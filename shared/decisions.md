# Keputusan Triage & Catatan Run

Keputusan bertanggal yang memengaruhi assertion di `tests/*.spec.js`. Spec hanya menyimpan komentar satu baris yang merujuk ke file ini.

| Tanggal | SCN | Keputusan | Alasan |
|---|---|---|---|
| 2026-08-22 | SCN-0004 | Assertion indikator step wizard memakai `.filter({ visible: true })` | Ada span stepper duplikat yang hidden di DOM (temuan run 20260822-0841). |
| 2026-08-22 | SCN-0007 | Precondition: pilih Tipe Pengiriman dulu sebelum assert field PIC | Field PIC baru dirender setelah Tipe Pengiriman dipilih (dikonfirmasi triage run 20260822-0858). |
| 2026-08-22 | SCN-0007 | **KEPUTUSAN user: BUKAN BUG** — assertion helper text diselaraskan ke perilaku live | Live menampilkan label field "PIC Pengirim \*" / "PIC Penerima \*" (bukan teks helper terpisah M-13 "Nama PIC Pengirim"), dan tidak ada teks contoh M-14 "Contoh: 081234567898" sama sekali — dikonfirmasi via probe manual, bukan masalah selector/precondition. Variasi copy yang diterima: AC-004/REQ-007 hanya mensyaratkan field Data Pengirim/Penerima tampil, bukan wording helper persis M-13/M-14. |
| 2026-08-22 | SCN-0011 | Klik langsung tombol Selanjutnya untuk memverifikasi blokir validasi; tetap ditandai **BUG (probable)** REQ-010/AC-007 | Dikonfirmasi 2x independen (harvest + run 20260822-0858): atribut `disabled` tidak ada di DOM saat field wajib kosong — tapi belum jelas apakah validasi tetap memblokir lewat jalur lain (toast/inline saat klik). Klik aman: navigasi wizard, tidak membuat/menyimpan order. |
| 2026-08-22 | SCN-0133 | **ASM-D03 RESOLVED via triage** — filter Tipe Pengiriman enabled tidak lagi diassert sebagai failure | Enabled penuh dikonfirmasi 2x independen (harvest + run SCN-0133 sebelumnya); tidak ada REQ yang mewajibkan disabled — styling-only, bukan bug. |
| 2026-08-22 | (semua skenario order yang masuk form buat order) | **KEPUTUSAN user: workaround resmi** — masuk halaman buat order via navigasi langsung `https://oms-staging.prahu-hub.com/order/buat` (`page.goto`), JANGAN bergantung klik tombol "Buat Order" | Tombol "Buat Order" di `/order` tidak bereaksi (reproducible 2x pada explore 2026-08-22, sebelumnya intermiten). Bug tombolnya tetap tercatat sebagai bug-candidate di module-map (Temuan Janggal #1) — workaround hanya agar skenario form tidak `blocked`. Skenario yang secara eksplisit menguji tombol itu sendiri tetap klik tombol & catat hasil apa adanya. |
| 2026-09-04 | (SUPERSEDES entri 2026-08-22 di atas) | **RESOLVED — tombol "Buat Order" kini berfungsi normal**, workaround navigasi langsung tidak lagi wajib (tetap aman dipakai) | Re-explore fokus modul Order (`/explore oms012-order-ftl-auto-stuffing`) mengonfirmasi klik tombol "Buat Order" dari `/order` langsung navigasi ke `/order/buat` dengan wizard Step 1 terbuka, tanpa workaround. Detail: `explore/module-map.md` Temuan Janggal #1. `tests/oms012-order-ftl-auto-stuffing.spec.js` masih pakai `page.goto('/order/buat')` langsung — tidak perlu diubah (hasil sama, tidak flaky), tapi komentar baris ~59 yang menyebut bug ini sudah usang. |
| 2026-08-23 | (modul oms014-order-ftl-fcl-normal, semua skenario) | **KEPUTUSAN user: pakai akun Admin Utama sebagai akun Shipper/Staff Operasional** — login test memakai `finance.roro1@gmail.com` (akun #1 di `config/env.md`) untuk precondition "User login sebagai Staff Operasional (Shipper)" | `config/env.md` baris akun #3 (role Shipper/Staff Operasional) belum diisi user (`ISI_DISINI`). User instruksi eksplisit: "pakai akun yang ada dulu shipper kita anggap admin" — gunakan akun Admin yang sudah ada sebagai pengganti sementara. Jika ada perbedaan hak akses/menu antara role Admin dan Shipper yang memengaruhi hasil skenario, catat sebagai bug-candidate/catatan, bukan failed murni. |
| 2026-08-23 | (modul oms014-order-ftl-fcl-normal, ditemukan di Batch 6, berlaku untuk SEMUA batch selanjutnya) | **WORKAROUND ENVIRONMENT WAJIB: pakai `locator.dispatchEvent('click')`, JANGAN `locator.click()` biasa** untuk semua button/checkbox/radio di wizard Buat Order (`/order/buat`) pada sesi Playwright MCP ini | Terkonfirmasi via diagnosis: `locator.click()` (mouse-based, real koordinat via CDP) tidak memicu handler React sama sekali di sesi ini — checkbox "Tambahkan Asuransi" dan tombol "Pilih Barang" tidak bereaksi (0 network request, DOM tidak berubah) meski `isVisible()`/`isEnabled()` true dan elementFromPoint mengonfirmasi target benar. `page.viewportSize()` mengembalikan `null` (indikasi sesi terhubung ke browser nyata via CDP, bukan browser yang di-launch Playwright sendiri) — kemungkinan penyebab: mismatch translasi koordinat mouse. `locator.dispatchEvent('click')` (dan raw DOM `el.click()` via `page.evaluate`) terbukti bekerja normal (modal terbuka, checkbox toggle, state React berubah). Fill/type text (`locator.fill()`) tidak terpengaruh (tidak berbasis koordinat mouse). **Executor batch berikutnya: langsung pakai `dispatchEvent('click')` untuk semua interaksi klik, jangan buang waktu mendiagnosis ulang.** |
| 2026-08-23 | OMS014-EDG-027 | **Tidak dianggap failed murni — dicatat sebagai gap-desain/bug-candidate untuk triage**: mencentang ulang "Tambahkan Asuransi" TIDAK mewajibkan isi ulang Nilai Barang | Skenario mengasumsikan uncheck→check ulang mengosongkan Nilai Barang (pesan "Nilai Barang harus diisi" harus muncul). Perilaku live: nilai lama (mis. "500.000") **tetap tersimpan** di background walau kolom disembunyikan saat unchecked, dan muncul kembali otomatis saat dicentang ulang — validasi tidak trigger, navigasi ke Step 3 berhasil. Berpotensi UX yang lebih baik (tidak kehilangan data), tapi bertentangan dengan assertion skenario. Perlu keputusan user apakah ini perilaku yang diterima atau perlu diselaraskan ke REQ-014. |
| 2026-08-23 | OMS014-EDG-003 | Bukan bug — hanya beda copy | Desain/skenario mengasumsikan 2 badge terpisah ("Kubikasi melebihi kapasitas armada" + "Berat melebihi kapasitas armada"). Live menampilkan **1 badge gabungan**: "Kubikasi dan Berat melebihi kapasitas armada". Non-blocking, REQ-016 tetap terpenuhi (tombol Selanjutnya tetap enabled) — dicatat sebagai catatan copy, bukan bug. |
| 2026-08-23 | OMS014-NEG-014 | **Ditandai blocked, bukan failed** — `Tanggal Permintaan Muat` adalah pure calendar-picker (`<div role="button">`), TIDAK ADA free-text input sama sekali | Diverifikasi via DOM: klik field membuka kalender (grid tanggal + scroll JAM/MENIT), tidak ada `<input type="text">` yang menerima ketikan bebas di manapun pada widget ini. Skenario mengasumsikan field bisa diisi string mask-invalid ("2026-07-24 14.30") via `.fill()` — secara struktural mustahil dieksekusi karena user hanya bisa memilih dari kalender, tidak bisa mengetik format sembarang. Precondition test tidak tercapai, bukan pass/fail perilaku aplikasi. |
| 2026-08-23 | OMS014-NEG-015, EDG-012, NEG-018, NEG-019 | **BUG CANDIDATE kuat, pola sistemik** — Step 3 Vendor&Harga: `Tanggal Permintaan Muat` (masa lalu), `PPN` (>100%), `PPh` (negatif) **semuanya diterima tanpa validasi** dan navigasi lanjut mulus ke Step 4 Review | Dikonfirmasi via kalender: navigasi mundur ke Mei 2020 tidak diblokir sama sekali (semua tanggal `disabled:false`), pilih `01/05/2020` lalu klik Selanjutnya → langsung ke Step 4 (REQ-017 dilanggar). PPN diisi `150` dan `100,1` → diterima, lanjut ke Step 4 (nilai bertahan "150" saat kembali ke Step 3, membuktikan bukan race condition/delay, tapi benar-benar tidak divalidasi). PPh diisi `-5` → diterima, lanjut ke Step 4. Kontras dengan `Vendor` kosong (NEG-013), `Harga` 0 (NEG-017), dan `Waktu Perjalanan` 0 (NEG-016) yang **benar tervalidasi** (navigasi tertahan + pesan `harus diisi`/`minimal`) — jadi validasi Step 3 tidak konsisten: field inti (Vendor/Harga/Waktu Perjalanan) tervalidasi, field komponen harga tambahan (Tanggal/PPN/PPh) tidak. |
| 2026-08-23 | OMS014-NEG-016, EDG-013 | Field `Waktu Perjalanan` hanya editable (dan hanya muncul) SETELAH `Vendor` dipilih, DAN hanya jika kombinasi rute (Drop Point Asal+Tujuan) belum ada di Master Waktu Perjalanan | Sebelum Vendor dipilih: field tidak dirender sama sekali (tidak ada label/input). Setelah Vendor dipilih, jika rute SUDAH ada di master → field tampil sebagai **teks read-only** "`N` Jam" (bukan input) tanpa alert. Jika rute BELUM ada di master → field jadi **input number editable** + alert kuning "Rute belum ada di Master Waktu Perjalanan...". Master data ini terkonfirmasi bersifat permanen lintas-sesi (rute yang sama dipakai run sebelumnya sudah read-only di run ini). Executor batch berikutnya yang butuh Waktu Perjalanan editable: pakai kombinasi Drop Point Asal/Tujuan yang belum pernah dipakai run manapun sebelumnya. |
| 2026-08-28 | (ad hoc — pembuatan order FTL/FCL/LTL/LCL) | **BUG-CANDIDATE**: checkbox `Gunakan komponen harga` (Step 3) defaultnya **TERCENTANG**, bukan default off | Terverifikasi berulang di 10 order (4 FTL + 4 FCL + LTL + LCL) yang dibuat ad hoc 2026-08-28 — PPN 11% + PPh 2% otomatis diterapkan ke Harga DPP tanpa aksi user apa pun. Konsisten dengan `FND-OMS015-05` (severity Major) di `results/_triage__oms015-order-ltl-lcl-universal__20260827-075803.md`. `shared/selector-map-order.md` (harvest 2026-08-22) tidak mencatat default state checkbox ini — perlu di-refresh. |
| 2026-08-28 | (ad hoc — order FTL/FCL) | Field `Pengirim`/`Penerima` (nama perusahaan) WAJIB diisi **SEBELUM** `Drop Point Asal`/`Tujuan` | Dropdown Drop Point terfilter sesuai company yang dipilih di field Pengirim/Penerima. Kalau urutan dibalik (isi Drop Point dulu, baru ganti company), Drop Point yang sudah terisi otomatis **ter-reset kosong** dengan pesan validasi "Drop Point harus dipilih" — tanpa toast peringatan, baru ketahuan saat submit Step 1 gagal. Executor berikutnya: selalu isi Pengirim/Penerima dulu → Drop Point → baru PIC/No. WhatsApp PIC (karena memilih Drop Point men-auto-fill PIC/WA dari data master, menimpa isian manual sebelumnya — perlu di-overwrite ulang SETELAH Drop Point selesai dipilih). |
| 2026-08-28 | (ad hoc — order FTL/FCL multi-alamat) | Validasi keunikan Drop Point bersifat **GLOBAL per-order**, bukan cuma pasangan Asal↔Tujuan yang berhadapan langsung | Ditemukan saat order Multipickup FCL: 2 blok Pengirim berbeda tidak boleh pakai Drop Point yang sama ("Drop Point Pengirim tidak boleh sama"), dan Drop Point Tujuan tidak boleh sama dengan Drop Point Asal MANAPUN di antara seluruh blok Pengirim (bukan cuma pasangan yang secara posisi berhadapan). Pesan error inline bisa muncul di blok yang bukan penyebab utama sehingga membingungkan saat debug manual. Perlu diperhatikan saat menyiapkan data test multi-alamat (Multipickup/Multidrop/Multipoint): pastikan SEMUA Drop Point di seluruh blok Pengirim+Penerima unik satu sama lain. |
| 2026-08-28 | (ad hoc — order FTL/FCL) | Validasi "Drop Point Tujuan tidak boleh sama dengan Drop Point Asal" bersifat **silent-block** | Tombol "Selanjutnya" Step 1 tetap terlihat enabled, tapi klik tidak menavigasi ke Step 2 saat ada Drop Point yang duplikat — tanpa toast global, pesan hanya muncul inline di bawah dropdown terkait. Terkait dengan baris "Validasi keunikan Drop Point bersifat GLOBAL per-order" di atas — sama-sama silent-block, cakupannya global. |
| 2026-08-28 | (ad hoc — order LTL) | **BUG-CANDIDATE** (konfirmasi ulang `FND-OMS015-01`): Drop Point Asal/Tujuan pada LTL difilter oleh Kota Asal/Kota Tujuan, kontra REQ-009 | Kombinasi kota sembarang (dicoba Medan→Palembang) menghasilkan dropdown Drop Point kosong total ("Tidak ada pilihan"), order tidak bisa dilanjutkan. Kombinasi yang terverifikasi **VALID** per 2026-08-28: Kota Surabaya → Kota Bandung, dan Kota Surabaya → Kota Balikpapan. Rujukan: `scenario/oms015-order-ltl-lcl-universal/oms015-order-ltl-lcl-universal.analysis.md` REQ-009, `results/_triage__oms015-order-ltl-lcl-universal__20260827-075803.md` FND-OMS015-01. |
| 2026-08-29 | (modul oms017-penugasan-tracking, TC-P-010, berlaku untuk SEMUA batch modul ini) | **KONFIRMASI: workaround `dispatchEvent('click')` (entri 2026-08-23) berlaku juga di modul Penugasan Tracking, BUKAN cuma wizard Buat Order** | TC-P-010 (tombol "Tambah Penugasan", login Vendor) awalnya ditandai **failed/bug-candidate** di run `_batch01__oms017-penugasan-tracking__20260829-102240.json` karena `locator.click()` biasa tidak menavigasi sama sekali (0 efek, tanpa error). Setelah verifikasi ulang dengan `locator.dispatchEvent('click')`: tombol langsung menavigasi ke `/penugasan-tracking/tambah`, heading & daftar "Pilih Order" tampil normal — verdict dikoreksi jadi **passed**. Hasil sudah di-overwrite di file JSON di atas. Elemen filter panel (Terapkan, dropdown Kota Asal/Vendor) di modul ini juga sempat butuh `force:true`/`dispatchEvent` (lihat TC-P-013 s.d. TC-P-016 di run yang sama). **Executor batch berikutnya di modul oms017: langsung pakai `dispatchEvent('click')` untuk SEMUA interaksi klik (tombol, dropdown, action menu row) sejak awal, jangan diagnosis ulang atau ditandai failed hanya karena `locator.click()` biasa tidak bereaksi.** |
| 2026-08-29 | (modul oms017-penugasan-tracking, Batch 7, form Tambah Penugasan screen 134/135) | **GOTCHA TEKNIS WAJIB DIBACA**: datepicker kalender (`div[role="button"]` + grid `button.h-9.w-9`) render baris terdepan berisi tanggal SISA BULAN SEBELUMNYA (contoh grid Agustus 2026 diawali `26 27 28 29 30 31` dari Juli) — kalau tanggal target ada di rentang 26-31, `locator(...).filter({hasText:/^26$/}).first()` BISA salah pilih sel Juli, bukan Agustus. Wajib pakai `.nth(1)` (bukan `.first()`) untuk tanggal 26-31, ATAU scoping ketat + cek header bulan (`text=/^(Januari|...|Desember)$/`) sebelum klik. Selain itu, sebagian instance datepicker (terutama field baris ke-N pada "Data Kapal Connecting" yang baru ditambah via "+ Tambah Kapal Connecting") **default terbuka di bulan Juli**, bukan bulan berjalan (Agustus) — cek header bulan setiap kali buka kalender baru, kalau bukan bulan target klik tombol navigasi (2 tombol ber-svg di header, `.last()` = next month) dulu sebelum klik tanggal. Kalender juga TIDAK auto-close setelah pilih tanggal+waktu (field dengan JAM/MENIT seperti Closing Time) — popup tetap terbuka inline sampai field lain diklik; field tanggal-only (ETD/ETA/ETD Connecting) auto-close setelah 1 klik tanggal. Saat 2 kalender terbuka bersamaan, selector tanggal global akan ambigu — scope ke container field spesifik via `label.filter({hasText:'<Label Field>'}).locator('xpath=..')` lalu cari `button.h-9.w-9` di dalamnya. Validasi ETD Connecting ternyata dua arah: `ETD Connecting minimal Berangkat (ETD)` DAN `ETD Connecting maksimal Tiba (ETA)` (REQ-049 hanya sebut batas atas) — keduanya inline error, tidak memblokir tombol Simpan awal secara konsisten (kasus 1 baris connecting: klik Simpan tidak membuka dialog konfirmasi sama sekali; kasus 3 baris dengan error di baris ke-3: dialog konfirmasi tetap terbuka, tapi klik Simpan final di dialog tetap tidak menyimpan/redirect — hasil akhir "data tidak tersimpan" tetap konsisten, hanya jalur UX beda). Field "No. Polisi/Jenis Armada" dan "Sopir/No. WhatsApp" master adalah custom combobox (`button[aria-haspopup=listbox]` + `role=option` hasil klik `dispatchEvent`), placeholder pencarian order di halaman ini persis `Cari order...` (huruf kecil, bukan `Cari Order` seperti asumsi ui-inventory). Field No. WhatsApp manual sopir punya placeholder `Contoh: 621234567898`, BUKAN `Masukkan No. WhatsApp` seperti asumsi. |

## 2026-08-29 — Triase run oms017-penugasan-tracking (runId 20260829-102240)

Hasil: 195 skenario — 112 passed, 22 failed, 37 blocked, 24 skipped (@stress). Detail: `results/_triage__oms017-penugasan-tracking__20260829-102240.md`.

- **Bug aplikasi (lapor dev)**: FND-OMS017-01 (filter AND Sopir+Jenis/ID → 0 baris), FND-02 (filter No. Kontainer tidak berfungsi), FND-03/06 (Batal sopir bongkar → daftar, REQ-066; verdict valid meski ada false-positive selector 'Ya' — URL akhir diukur langsung), FND-05 (.gif diterima), FND-10 (pop-up riwayat tanpa strip "-"), **FND-11 (Edit FCL tidak mengunci field setelah Selesai Muat, REQ-077/078/079 — prioritas tinggi)**, **FND-12 (ETD/ETA tersimpan mundur 1 hari — dugaan timezone backend)**, FND-15 ("Kabupaten" vs "Kab.", REQ-028, 3 layar), minor: FND-04, FND-14.
- **Gap desain / klarifikasi PO**: FND-07 (unit "4 MB"), FND-08 (dialog Batal sopir bongkar), FND-09 (tab Per Tahapan FCL), FND-13 (badge status di Detail).
- **Bukan bug**: TC-P-010 (tombol Tambah Penugasan) = workaround dispatchEvent; DOM quirk `table tbody tr` menyertakan 1 baris kosong.
- **Blocked karena data/akun (bukan cacat)**: butuh ≥20 baris penugasan (pagination), order metode CY-*/Door-CY (tidak ada satu pun di staging), akun Shipper-scoped (akun #3 env.md kosong), Driver Hub.
- Data test tercipta: Master Pelayaran `AUTOTEST20260829PELAYARAN` (field menstrip '-'), belasan penugasan ORD* (daftar di file triase) — boleh dibersihkan.
- Catatan proses: eksekusi mengubah urutan plan (Grup B Tambah Penugasan dijalankan sebelum Batch 2-3) agar data tersedia; file plan tersimpan sebagai `_plan__oms017-penugasan-tracking__20260829-143000.md` sedangkan runId hasil = 20260829-102240.

## 2026-09-02 — Harvest selector `/tracking` (oms022-public-tracking-improve), sebelum eksekusi modul

Hasil: `shared/selector-map-tracking.md`. Modul ini tidak punya `ui-inventory.md` (hanya `.analysis.md`/`.feature`/`.scenarios.json`), dan `analysis.md` sendiri menandai "designs: TIDAK ADA — seluruh selector adalah USULAN" — sehingga dilakukan reality-check browser dulu sebelum `/test-module` lanjut ke eksekusi 98 skenario, karena ditemukan gap besar:

- **Tidak ada `data-testid`/`data-state`/`data-history-type`** di implementasi sama sekali — seluruh `selectorCatalog`/`selectorHints` di `oms022-public-tracking-improve.scenarios.json` (berbasis testid) tidak berlaku, ganti pakai role/text di `selector-map-tracking.md`.
- UI pakai istilah **"Nomor Perjalanan"**, bukan "No. Resi" (field API tetap `resi`, jadi konsep di scenarios.json masih valid, cuma label UI beda).
- **Alur nyata 3 langkah dengan 1 langkah tersembunyi yang sama sekali tidak dimodelkan di dokumen skenario**: submit nomor perjalanan → kartu ringkas status → klik "Lihat Detail" → **modal verifikasi 4 digit terakhir No. WhatsApp penerima** → baru redirect ke `/tracking/<no-perjalanan>` (stepper + riwayat). Mayoritas skenario "positive" di scenarios.json expect hasil langsung setelah submit form — tidak akan match alur nyata apa adanya.
- Seluruh 16 `testDataSeeds` (`LKL...`) di scenarios.json **tidak ada di database staging** (semua → "Nomor perjalanan tidak ditemukan"). Data uji nyata pengganti: No. Perjalanan `TRC69283535` (order `ORD8160410409`, FCL, Delivered), 4 digit verifikasi `1881` — cara ambil nomor lain didokumentasikan di `selector-map-tracking.md`.
- Regex `assertionRules` doc untuk histori "sintetis"/"bongkar" **sebagian tidak cocok** data nyata (event nyata lebih variatif: "tiba di kota X", "penyebrangan menuju X", dst — di luar 3 kategori muat/sintetis/bongkar yang diasumsikan; teks "dibongkar"/"selesai bongkar" tidak ditemukan sama sekali pada order Delivered yang dicek).
- Kabar baik: stepper 3 tahap (Pick Up/On Delivery/Delivered, bahasa Inggris) match asumsi doc; heading "Riwayat Pengiriman" + role list/listitem match doc; endpoint `POST /api/v1/public/tracking/search` body `{"resi":[...]}` konsisten dgn konsep "resi" di doc.
- **KEPUTUSAN user**: harvest selector dulu sebelum lanjut eksekusi (bukan langsung jalankan 98 skenario apa adanya) — lihat opsi yang ditawarkan saat `/test-module oms022-public-tracking-improve` dijalankan. Eksekusi penuh belum berjalan per catatan ini; scenarios.json kemungkinan perlu penyesuaian expected-flow (bukan cuma selector) sebelum hasil failed/blocked bisa ditriage dengan bermakna.

## 2026-09-02 — Triase run oms022-public-tracking-improve (runId 20260902-094941)

Hasil: 98 skenario — 48 passed, 14 failed, 20 blocked, 16 skipped (@stress). Detail: `results/_triage__oms022-public-tracking-improve__20260902-094941.md`. Report: `reports/oms022-public-tracking-improve__20260902-094941.xlsx`.

- **Bug aplikasi (lapor dev), prioritas tinggi**: **FND-OMS022-02** — state error sistem (UI-T09) sama sekali tidak terimplementasi; kegagalan API menghasilkan silent failure total (halaman diam, tanpa pesan/retry). **FND-OMS022-01** (10 skenario, REQ-023/024) — entri riwayat "sintetis" (transit pasca-muat-sebelum-bongkar) tidak pernah muncul di order multi-alamat manapun; EDG-024 membuktikan pelanggaran invariant stepper-vs-riwayat secara langsung. **Caveat**: order multi-alamat yang diuji semuanya punya timestamp riwayat identik per order (indikasi seeding massal) — perlu verifikasi dengan order non-seed sebelum eskalasi final sebagai bug pasti (lihat detail §2 file triase).
- **Gap desain / klarifikasi PO**: FND-OMS022-03 (Enter tidak submit), FND-OMS022-04 (tombol Hapus tidak reset kartu hasil).
- **Bukan bug**: FND-OMS022-05 — fitur multi-resi (maks 10, chip-based) sama sekali tidak dimodelkan dokumen skenario; pencarian kedua menambah bukan mengganti hasil, perilaku ini disengaja & konsisten.
- **Blocked (20)**: 14 butuh state parsial/in-progress spesifik yang tidak ada di staging (order yang tersedia sudah fully delivered), 3 butuh tooling seed/API untuk sisip event bertahap (di luar kapasitas browser read-only), 3 data spesifik dikonfirmasi tidak ada (order `ORD-20260607009`, order Dibatalkan dengan No. Perjalanan aktif, order dengan entri riwayat tanpa lokasi).
- **Temuan reusable untuk eksekusi berikutnya di `/tracking`**: `locator.dispatchEvent('click')` wajib untuk tombol Lacak Pengiriman/Lihat Detail/Verifikasi (pola sama seperti wizard Buat Order 2026-08-23 & Penugasan Tracking 2026-08-29, sekarang terkonfirmasi juga di halaman publik ini). Progres stepper reached/current/pending BISA dibedakan lewat class Tailwind (`bg-brand-500`/`bg-brand-100`/`bg-gray-100`) meski tidak ada `data-state`. Detail lengkap: `shared/selector-map-tracking.md` section "Update pasca-eksekusi".
- **Gotcha baru `/order` (ditemukan saat riset data)**: daftar order default render 20 baris meski dropdown "Tampilkan" menunjukkan pilihan lebih besar dan total data lebih banyak — perlu diklik ulang/verifikasi sebelum scan penuh. Dropdown menu "⋮ Aksi" per baris di `/order` adalah `position:fixed`, tidak selalu muncul di accessibility snapshot biasa (perlu screenshot atau query DOM langsung untuk verifikasi).
- Data order real yang ditemukan untuk testing lanjutan (pengganti testDataSeeds fiktif `LKL...`): lihat tabel lengkap di `shared/selector-map-tracking.md` (No. Perjalanan `TRC...` + 4 digit verifikasi HP untuk tipe Normal, Multi Pick Up 2/3, Multi Drop 2, Multipoint 2x2/3x3, order riwayat kosong, order in-progress).

## 2026-08-29 — Task ad hoc: 12 order FTL (Normal/Multipickup/Multidrop/Multipoint × 1/2/3 armada) → penugasan → tracking sampai Terkirim

Log: `results/_task__ftl-orders__20260829.json`; report: `reports/task-ftl-orders-penugasan-tracking__20260829-160500.xlsx`; resep alur lengkap: `shared/recipe-ftl-order-flow.md`.

- **BUG-CANDIDATE FND-TASK-01 (double submit)**: klik "Simpan" di Step 4 (Review) tidak memberi feedback UI (dialog "Order Berhasil Dibuat"/redirect) selama ±2,5 detik padahal `POST /api/order` sudah 201 → klik kedua membuat **order duplikat identik** (`ORD7997900707`, masih "Menunggu Penugasan", **perlu dibatalkan manual oleh user** — aksi Batalkan Order diblokir classifier auto-mode). Executor berikutnya: tunggu response network `POST /api/order` (201) sebelum memutuskan klik ulang, jangan andalkan perubahan DOM.
- **BUG-CANDIDATE**: dropdown No. Polisi master di form Tambah Penugasan **tidak memfilter nopol yang sudah dipakai card lain** dalam order yang sama — nopol duplikat bisa lolos ke Simpan; cek manual sebelum Simpan.
- Perilaku (bukan bug): sopir master yang sama ("Mayora") boleh dipakai berulang lintas card/penugasan tanpa penolakan; tracking berjalan **per armada** (1 baris penugasan per armada, masing-masing punya progres Selesai Muat/Bongkar sendiri); Drop Point penerima/pengirim dalam satu kota digrup otomatis di form tracking ("N/N alamat selesai"); keunikan Drop Point hanya **per order** (boleh reuse lintas order).
- Jebakan wizard: saat rute belum ada di Master Waktu Perjalanan, field "Waktu Perjalanan" jadi input editable dan **bergeser ke SEBELUM Harga DPP** dalam urutan `getByPlaceholder('0')` — jangan pakai `.first()` buta. Modal "Pilih Barang" bisa race-condition saat banyak section (Multipickup 3 armada × 3 pickup) — beri jeda ±650 ms per modal dan terima SKU apa pun yang ter-klik.

## 2026-09-02 — Task ad hoc: 10 order (FTL Normal/Multipickup/Multidrop/Multipoint, FCL Normal/Multipickup/Multidrop/Multipoint, LTL, LCL) → semua "Menunggu Penugasan"

Log: `results/_task__orders-ftl-fcl-ltl-lcl__20260902.json`. Hasil: **10/10 sukses**, semua `POST /api/order` 201 dengan `success:true`, tanpa retry/blocked. Kode order: FTL Normal `ORD8330780552`, Multipickup `ORD8330884531`, Multidrop `ORD8330947448`, Multipoint `ORD8331016843`; FCL Normal `ORD8331138575`, Multipickup `ORD8331211384`, Multidrop `ORD8331276463`, Multipoint `ORD8331347620`; LTL `ORD8331456722`; LCL `ORD8331533103`.

- **KOREKSI/PERBAIKAN formula index field Harga Step 3** (memperjelas recipe FTL 2026-08-29 yang ambigu dan sempat menyesatkan): field `getByPlaceholder('0')` di Step 3 SELALU berisi **Harga, PPN%, PPh%** sebagai 3 elemen terakhir (urutan tetap), dan opsional **Waktu Perjalanan** di depan semuanya bila rute baru. Jadi index Harga yang benar = `count - 3` (bukan `.first()` atau asumsi index tetap 0/1) — terbukti benar di seluruh 10 order (count=3 → hargaIdx=0 [Harga,PPN,PPh]; count=4 → hargaIdx=1 [Waktu,Harga,PPN,PPh]). **Insiden nyaris salah data**: pada order FTL Normal pertama, sempat salah isi `priceBoxes.nth(1)` dengan asumsi lama "nth(1)=Harga saat count>=2" — ternyata nth(1) adalah PPN%, sehingga PPN sempat ter-set ke `1.500.000%` dan Harga kosong (tertangkap sebelum submit karena field Harga menampilkan error "Harga harus diisi" di Review, langsung dikoreksi). Executor berikutnya: JANGAN pakai index harcoded 0/1, selalu hitung `count - 3`.
- **FCL Multipickup/Multidrop/Multipoint (belum pernah dieksekusi sebelumnya)**: struktur Step 1 dan Step 2 **identik 1:1 dengan pola FTL** yang sudah terdokumentasi di `shared/recipe-ftl-order-flow.md` — label blok "Pengirim 1/2.."/"Penerima 1/2..", pool `.nth(0)` reset per blok terisi, sub-section Step 2 "Pick Up i"/"Drop Off j"/matrix "Pick Up i - Drop Off j", checkbox "Tambahkan Asuransi" 1 per **Kontainer** (bukan per armada, tapi mekanismenya sama — index `checkbox.nth(jumlahKontainer)` = item pertama modal). Tidak ditemukan perbedaan/jebakan baru dibanding pola FTL; validasi keunikan Drop Point GLOBAL per-order tetap berlaku sama. FCL Multipoint (2 Pengirim x 2 Penerima, matrix 4 tombol Pilih Barang) **tidak mengalami race double-submit** di Step 4 (beda dari kasus FTL Multipoint 2026-08-29) — kemungkinan karena volume data lebih kecil (1 kontainer vs 3 armada), tapi tetap disarankan pakai listener `page.on('response')` sebagai default aman untuk SEMUA tipe order, bukan cuma Multipoint.
- **TEMUAN BARU — urutan field LTL/LCL TERBALIK dari FTL/FCL**: untuk LTL dan LCL, label visual Step 1 menampilkan **"Drop Point Asal *" SEBELUM "Pengirim"** (dan "Drop Point Tujuan *" sebelum "Penerima"). Perilaku aktual mengonfirmasi urutan ini: memilih **Drop Point Asal/Tujuan langsung** (tanpa pilih company dulu) **AUTO-MENGISI field Pengirim/Penerima (company)** — kebalikan dari FTL/FCL yang mewajibkan pilih company dulu baru Drop Point ter-filter. Executor berikutnya untuk LTL/LCL: pilih Kota Asal/Tujuan (LTL) atau Pelabuhan Asal/Tujuan (LCL) dulu, lalu langsung pilih **Drop Point Asal/Tujuan** dari dropdown (list tidak terfilter oleh company), company akan terisi otomatis — TIDAK PERLU dan tidak ada tombol pemilihan company terpisah yang wajib diklik lebih dulu seperti FTL/FCL. PIC/No. WhatsApp PIC tetap perlu diisi/overwrite SETELAH Drop Point (sama seperti FTL/FCL, karena auto-fill dari master).
- **LTL Surabaya→Bandung: company Borneo/Trimurti TIDAK VALID untuk rute ini** (area mereka Balikpapan/Surabaya-Jatim, bukan Bandung). Drop Point Asal yang valid utk Kota Surabaya antara lain `IK - Kota Lama Surabaya`, `IK - Balai Pemuda` (keduanya milik PT. Trimurti Jayandaru), `EL - Droppoint Surabaya Haier`, dll. Drop Point Tujuan utk Kota Bandung **hanya ada SATU pilihan**: `EL - Droppoint Bandung Haier` (company **PT. Haier Sales Indonesia**) — dropdown tidak menampilkan opsi lain sama sekali untuk kota ini. Kombinasi yang terbukti jalan: Pengirim PT. Trimurti Jayandaru (IK)/`IK - Kota Lama Surabaya` → Penerima PT. Haier Sales Indonesia/`EL - Droppoint Bandung Haier`.
- **Konfirmasi ulang (tidak ada temuan baru)**: FCL Normal Step 3 tetap tanpa field Waktu Perjalanan (dicek eksplisit via body-text scan, bukan cuma asumsi — hasil "Waktu Perjalanan" yang match hanya menu sidebar "Master Waktu Perjalanan", false-positive regex biasa terjadi kalau tidak discope). LCL Drop Point tetap TIDAK difilter oleh Pelabuhan Asal/Tujuan (semua DP Borneo/Trimurti tetap muncul penuh apa pun pelabuhan yang dipilih). Checkbox "Gunakan komponen harga" default tercentang, PPN 11%/PPh 2% otomatis — konsisten di seluruh 10 order baru ini.

## 2026-09-02 (~15:20 WIB) — Task ad hoc lanjutan: 5 order (FTL x4 + LTL) vendor PT. Intan Emas Permata (IK) sukses; **FCL & LCL terblokir — BUG-CANDIDATE PRIORITAS TINGGI**

Log: `results/_task__orders-ftl-fcl-ltl-lcl-intan-emas__20260902.json`. Permintaan user: ulang 10 order (FTL/FCL Normal-Multipickup-Multidrop-Multipoint + LTL + LCL) yang sama seperti run `_task__orders-ftl-fcl-ltl-lcl__20260902.json` (13:46 WIB, vendor PT. Jaya Karsa (IK)), tapi vendor diganti **PT. Intan Emas Permata (IK)** (alias `IEPIK`, status Aktif, terverifikasi ada di `/manajemen-vendor`). Data Pengirim/Penerima/Drop Point reuse kombinasi yang sama (valid, DP unique-per-order jadi boleh dipakai ulang lintas order). 5/10 order sukses: FTL Normal `ORD8337126302`, Multipickup `ORD8337194621`, Multidrop `ORD8337252940`, Multipoint `ORD8337312450`; LTL `ORD8337730685` — semua `POST /api/order` 201 `success:true`, `vendorName` terkonfirmasi `PT. Intan Emas Permata (IK)` di response.

- **BUG-CANDIDATE BARU (belum ada FND-xx, prioritas tinggi)**: Wizard `/order/buat` Step 1 "Jenis Pengiriman dan Rute" **hanya menampilkan 2 kartu (FTL, LTL)** — kartu **FCL dan LCL hilang total dari DOM**, bukan cuma disabled/collapsed. Dikonfirmasi via `document.querySelectorAll('button.rounded-xl.border-2')` (grid `grid-cols-2`, `childCount:2`) dan screenshot (`.playwright-mcp/page-2026-09-02T08-27-45-381Z.png`). Ini KONTRADIKSI langsung dengan: (a) desain/`ui-inventory` yang menyebut 4 kartu (`FTL/FCL/LTL/LCL`, "Radio card ×4"); (b) `shared/selector-map-order.md` SCR-04/05 yang mendokumentasikan `getByRole('button', {name: /FCL/})` bekerja; (c) run **hari yang sama jam 13:46 WIB** (`_task__orders-ftl-fcl-ltl-lcl__20260902.json`, dikonfirmasi lewat sesi lain `omstest-94`) yang BERHASIL membuat 4 order FCL + 1 LCL lewat wizard ini.
- **Diagnosa yang sudah dicoba (semua negatif/tidak ditemukan penyebab)**: hard reload (`networkidle`) — tetap 2 kartu; klik tombol "Buat Order" asli dari `/order` (bukan direct-nav ke `/order/buat`) — hasil sama; cek `localStorage` — tidak ada key relevan (cuma `theme`); cek console errors — 0 error asli (1 error yang muncul cuma dari fetch manual saya sendiri tanpa auth header, false lead); cek network saat page load — `/api/pelabuhans` dan `/api/jenis-kontainers` (data yang harusnya dipakai FCL/LCL) tetap ke-fetch sukses status 200, jadi bukan data master yang hilang; cek `/setting/sistem` — tidak ada toggle terkait moda Darat/Laut atau FCL/LCL; cek `/api/quota/summary` — `used:3/base:200`, jauh dari limit, dan angka ini TIDAK naik meski sudah 4 order baru dibuat sesi ini (kemungkinan widget kuota tidak realtime, tidak terkait masalah kartu). Tidak ditemukan query param, tab, radio, select, atau elemen tersembunyi lain yang mengontrol kartu ini — `outerHTML` penuh section "Jenis Pengiriman dan Rute" hanya berisi 2 `<button>`.
- **Kesimpulan sementara**: kemungkinan regresi live di staging (deploy/config berubah antara 13:46–15:20 WIB) yang menonaktifkan FCL & LCL dari wizard Buat Order, ATAU efek samping dari aktivitas 2 sesi Claude lain (`omstest-53`, `omstest-94`) yang berjalan paralel di environment yang sama hari ini — penyebab pasti TIDAK terverifikasi, perlu dicek oleh tim dev OMS atau di-retry di sesi berikutnya (sempat dicek ulang ~5 menit kemudian, hasil masih sama/belum pulih).
- **Dampak ke task ini**: order FCL (Normal/Multipickup/Multidrop/Multipoint) dan LCL untuk vendor PT. Intan Emas Permata (IK) **belum bisa dibuat** — bukan gagal validasi/data, tapi opsi jenis order-nya sendiri tidak tersedia di UI. Executor berikutnya: cek dulu apakah kartu FCL/LCL sudah muncul lagi di `/order/buat` sebelum retry 5 order sisanya (Normal/Multipickup/Multidrop/Multipoint FCL + LCL), reuse data Pengirim/Penerima/DP dan pola `Pilih Barang`/`checkbox.nth(jumlahKontainer)` dari catatan run 13:46 di atas — hanya vendor yang perlu diganti ke PT. Intan Emas Permata (IK).

## 2026-09-02 (lanjutan, sesi retry) — 5 order FCL/LCL sisa berhasil dibuat, blocker kartu TIDAK reproduce lagi

Log lengkap (10/10 order, digabung dgn 5 sebelumnya): `results/_task__orders-ftl-fcl-ltl-lcl-intan-emas__20260902.json`. Order baru: FCL Normal `ORD8338374164`, Multipickup `ORD8338534214`, Multidrop `ORD8338620381`, Multipoint `ORD8338721286`; LCL `ORD8338814778` — semua `POST /api/order` 201 `success:true`, vendor PT. Intan Emas Permata (IK), tidak ada duplikat (diverifikasi via `browser_network_requests`, masing2 tepat 1 POST unik).

- **Blocker kartu FCL/LCL hilang (entri di atas) TIDAK reproduce di sesi ini** — begitu dicek ulang, Step 1 langsung menampilkan 4 kartu normal (FTL/FCL/LTL/LCL). Kesimpulan: kemungkinan besar **transient/race-condition render** (atau efek samping sesi paralel lain yang sudah selesai), bukan regresi permanen. Penyebab pasti tetap tidak diketahui — kalau muncul lagi di sesi mendatang, cek ulang sebelum mengasumsikan ini bug permanen yang sama.
  - **Reconfirmed 2026-09-04** (`/explore oms013-order-fcl-auto-stuffing`): 4 kartu tetap normal, `document.querySelectorAll` menghitung FTL/FCL/LTL/LCL lengkap. 2 hari berturut-turut tanpa reproduce — modul oms013 aman dieksekusi.
- **TEMUAN BARU**: field **"Tanggal Permintaan Muat" di Step 3 WAJIB diisi untuk FCL DAN LCL juga** (bukan cuma FTL/FCL yang sudah didokumentasikan sebelumnya) — field ini terpisah dari "Waktu Perjalanan" (yang memang absen total di FCL/LCL, itu benar). Kosongkan → toast inline "Tanggal Permintaan Muat harus diisi", tertahan di Step 3. Pola pengisian sama seperti resep FTL (`shared/recipe-ftl-order-flow.md` Step 3): klik teks `DD/MM/YYYY hh:mm` → `button.h-9.w-9.nth(31)` (tgl 25 bulan berjalan) → `button.tabular-nums.nth(10)` + `.nth(24)` (jam/menit) → klik heading "Vendor dan Harga" untuk menutup kalender.
- **GOTCHA TEKNIS BARU**: `locator.dispatchEvent('click')` pada tombol "Simpan" Step 4 kadang **hang/timeout ~30 detik** pada promise-nya, padahal klik itu sendiri TETAP sukses memicu submit (POST 201) + redirect — terjadi 1x (order FCL Normal), order tetap valid tanpa duplikat. Mitigasi yang terbukti aman untuk 4 order berikutnya: bungkus `dispatchEvent('click')` dengan timeout pendek (~6s) + try/catch, lalu verifikasi hasil lewat `browser_network_requests` (network layer) — JANGAN menunggu promise `dispatchEvent` resolve sebagai sinyal sukses/gagal.

## 2026-09-02 (lanjutan) — Tambah Penugasan 10 order Intan Emas: **BLOCKED total — BUG-CANDIDATE PRIORITAS TINGGI, vendor-scoping salah di endpoint Pilih Order**

Task: tambahkan penugasan (No.Polisi/Sopir utk FTL/LTL, No.Kontainer/Segel/Armada/Jadwal Kapal utk FCL/LCL) ke 10 order vendor PT. Intan Emas Permata (IK) yang baru dibuat (lihat entri di atas + entri "5 order FCL/LCL sisa"). **0/10 berhasil** — bukan gagal isi form, tapi order-nya sendiri tidak bisa dipilih/ditemukan di halaman `/penugasan-tracking/tambah`.

- **Root cause terkonfirmasi via `browser_network_requests`**: field pencarian "Pilih Order" di Tambah Penugasan memanggil `GET /api/orders?hasPenugasan=false&status=DRAFT,ASSIGNED,MENUNGGU_PENUGASAN&...` (endpoint JAMAK `orders`, beda dari `GET /api/order` tunggal yang dipakai Daftar Order). Response endpoint ini — dengan search string apa pun, termasuk kosong — **HANYA PERNAH mengembalikan order dari 1 vendor: `PT. Wahana Logistik`** (vendorId `361ac8fc-...`, 28 order), walau database punya 127 order status "Menunggu Penugasan" lintas banyak vendor (dikonfirmasi lewat `/order` + filter Status).
- Diuji eksplisit dengan 3 ID order dari 2 vendor lain (bukan Wahana): `ORD8337126302` (Intan Emas), `ORD8331347620` & `ORD8330780552` (PT. Jaya Karsa) — **ketiganya "Tidak ada order tersedia"**, walau valid & Menunggu Penugasan di `/order`. `createdById` order Intan Emas SAMA dengan order Wahana (sama-sama dibuat admin `19b31c0b-...`) — jadi **bukan** filter by-creator, tampak seperti vendor scoping yang salah/hardcoded ke 1 vendor di endpoint `/api/orders`.
- **Dampak**: kemungkinan besar BUKAN spesifik vendor Intan Emas — fitur Tambah Penugasan tampak rusak untuk order dari vendor mana pun SELAIN PT. Wahana Logistik. Perlu dilaporkan ke dev prioritas tinggi (blocking, bukan cuma 10 order task ini).
- **Fallback akun Vendor gagal dieksekusi**: percobaan login sebagai `pengirim.ph2021@gmail.com` (setelah logout Admin, di context browser yang sama — bukan storageState) **diblokir 2x oleh Claude Code auto-mode classifier** ("Permission for this action was denied"), bukan gagal kredensial. Dihentikan sesuai aturan "login gagal 2x → berhenti" — lihat `[[project-classifier-blocks-cancel-order]]` (memory, sudah diupdate mencakup pola ini juga, bukan cuma cancel-order/final-submit). Sesi browser berakhir logged-out di `/login`, tidak ada aksi destruktif.
- **Koreksi selector**: tombol Logout yang benar adalah **"Logout"** persis — `getByRole('button',{name:'Keluar'})` SALAH-MATCH ke tombol dashboard "Keluar Jalur 0" (tidak melakukan logout sama sekali). Perlu dicek apakah ini juga relevan untuk `explore/module-map.md` bagian info login.
- **Belum sempat diverifikasi**: struktur live form Tambah Penugasan untuk FCL/LCL (REQ-039..049 desain vs live) — blocker terjadi SEBELUM tahap isi form (di tahap pilih order), jadi field FCL/LCL tetap belum ter-harvest sama sekali.
- **Status 10 order**: semua tetap "Menunggu Penugasan" di `/order`, tidak berubah, tidak ada penugasan tertambah. Perlu keputusan user: tunggu fix dev untuk `/api/orders`, atau user login manual sebagai Vendor secara interaktif (agar classifier tidak memblokir) untuk melanjutkan investigasi/penugasan dari sisi situ.
- **[RESOLVED di sesi yang sama, lihat entri berikutnya]** — semua 10 penugasan akhirnya berhasil via VENDOR PORTAL.

## 2026-09-03 — Tambah Penugasan 10 order Intan Emas SUKSES 10/10 via VENDOR PORTAL (login interaktif user-approved)

Log: `results/_task__penugasan-intan-emas__20260902.json`. Semua 10 order (4 FTL + 4 FCL + LTL + LCL) berhasil ditugaskan, `POST https://apioms-staging.prahu-hub.com/api/penugasan` 201 `success:true` per order, status baris di `/penugasan-tracking` = **"Belum Berangkat"** semua. Armada `L9721IK - CDE` + sopir `Loekman - 6283830011881` dipakai berulang lintas 10 order tanpa penolakan.

- **TEMUAN KUNCI — VENDOR PORTAL**: akun #2 env.md (`pengirim.ph2021@gmail.com`) ternyata adalah **akun vendor PT. Intan Emas Permata (IK)** (header portal: "PT. Intan Emas Permata (IK) / Vendor", inisial IEPIK). Login diarahkan ke `/vendor-portal/order` (route berbeda dari admin `/order`). Menu vendor: Order (read-only + Detail), **Penugasan Tracking (route SAMA `/penugasan-tracking`)**, Master Operasional (Master Armada `/vendor-portal/master/armada`, Master Sopir), Akun Saya, Pusat Notifikasi. Semua 10 order Intan Emas TAMPIL di sisi vendor (baik daftar Order maupun pencarian "Pilih Order" di Tambah Penugasan) — **workaround resmi utk bug-candidate `/api/orders` sisi admin (entri sebelumnya): tugaskan order dari sisi vendor pemilik order**. Login vendor dilakukan di sesi utama interaktif atas instruksi user (classifier memblokir bila dari background agent).
- **Master vendor Intan Emas**: 6 armada semuanya jenis CDE (`L9721IK`, `L3735IK`, `L1235IK`, `L5726IK`, `L6646IK`, `L3520IK`), 1 sopir (`Loekman - 6283830011881`). Dropdown armada terfilter otomatis sesuai jenis armada order (REQ-033 confirmed).
- **FITUR BARU DITEMUKAN — "Pembatasan Kelayakan Armada"**: saat aktif, armada dgn masa berlaku KIR/Pajak(STNK) belum lengkap TIDAK bisa dipakai penugasan — klik Simpan memunculkan modal **"Armada Tidak Dapat Digunakan"** berisi detail KIR/Pajak + form inline "Perbarui Masa Berlaku" (3 input flatpickr readonly `dd/mm/yyyy`; diisi via API `input._flatpickr.setDate(dateObj, true)` — pakai objek Date, string ISO SALAH parse jadi tanggal ngaco) + tombol "Pilih Armada Lain"/"Perbarui dan Gunakan Armada". Semua 6 armada Intan Emas datanya belum lengkap → tanpa intervensi tidak ada armada yang lolos. **Klik "Perbarui dan Gunakan Armada" DIBLOKIR classifier Claude Code** (update master data). Resolusi run ini: **user mematikan sementara pengecekan KIR/STNK dari sisi mereka** → dropdown armada langsung bersih dari warning, penugasan lolos tanpa update master. Data master armada TIDAK jadi diubah oleh run ini.
- **Form Tambah Penugasan FCL/LCL live TERKONFIRMASI sesuai desain REQ-039..049** (harvest pertama kali): per card Kontainer = `No. Kontainer*` (`getByPlaceholder('Masukkan No. Kontainer')`), `No. Segel*`, `Armada Muat*` (master/manual, tombol sama "Pilih No. Polisi/Jenis Armada"), `Sopir Muat*`, Mode Penugasan (Tugaskan ke Pengurus); lalu 1x per penugasan: `Jenis Jadwal Kapal*` (radio Direct/Connecting), section "Detail Kapal Utama": `Pelayaran*` (dropdown master, opsi live: MERATUS, AUTOTEST20260829PELAYARAN), `Nama Kapal*`, `Voyage*`, `Closing Time*` (kalender custom + JAM/MENIT), `Berangkat (ETD)*`, `Tiba (ETA)*` (kalender date-only). LTL = identik FTL (1 card Nopol+Sopir; REQ-030/037/038 confirmed). LCL = identik FCL. Order multi-alamat TIDAK menambah card (card = jumlah armada/kontainer saja).
- **GOTCHA KALENDER VENDOR-PORTAL (beda dari catatan oms017 sisi admin!)**: kalender Closing Time/ETD/ETA **TIDAK auto-close saat field/trigger lain diklik** — bisa 3 kalender terbuka sekaligus (grid `button.h-9.w-9` jadi 85-127 elemen), dan selector global (`.first()`/`.nth(1)`) NYASAR ke kalender pertama di DOM (Closing Time sempat berubah 2x tanpa sengaja). **Wajib scope per-container**: dari `label` (hasText nama field) naik ke parent sampai ketemu container dgn `>=35 button.h-9.w-9`, lalu klik sel via native DOM `.click()` di dalam container itu (terbukti reliable utk 5 order FCL/LCL). Escape TIDAK menutup kalender ini. Catatan minor: klik jam/menit (`button.tabular-nums` idx 10 & 24 dlm container) dgn 3 kalender terbuka kadang tidak nempel — Closing Time 4 order terakhir jatuh ke default "hari ini 00:00" (tetap diterima validasi, POST 201; kalau butuh jam presisi, isi Closing Time saat HANYA kalendernya sendiri yang terbuka, seperti order FCL pertama).
- **Nilai tanggal aman yang terbukti lolos**: Closing Time 24/09/2026 10:00 (atau default hari-ini 00:00), ETD 25/09/2026, ETA 30/09/2026 (klik "30" pakai match TERAKHIR dlm container — match pertama = sisa bulan lalu di baris 1 grid). Tidak ada validasi yang menolak Closing Time < hari H muat maupun kombinasi ini.
- Dialog konfirmasi submit: heading **"Konfirmasi Penugasan"** ("Pastikan data penugasan sudah benar..."), tombol `Periksa Kembali` / `Simpan` — pola 2x Simpan (`.first()` lalu `.last()`) tetap berlaku.

## 2026-09-05 — `/explore oms013-order-fcl-auto-stuffing` (lanjutan): celah layar Ditugaskan / Step 3 / Edit Order terisi, 0 data ditulis

Detail lengkap: `explore/module-map.md` section "Detail Layar Modul Order — Varian FCL, lanjutan 2026-09-05" (13 screenshot `order-fcl-*`). Catatan yang MEMENGARUHI executor oms013 berikutnya:

- **Route Detail/Edit Order = UUID internal** (`/order/{uuid}`), bukan kode ORD. Direct-nav `/order/ORD7556205186` → `GET /api/order/ORD7556205186` **400** + "Gagal memuat data order." Executor: masuk lewat menu Aksi "Detail"/"Edit" atau klik baris; jangan bangun URL dari kode ORD.
- **Order FCL Ditugaskan kini 4**: `ORD7556205186` (Normal, **2 kontainer**, No. Perjalanan `TRC11182633`/`TRC90557595`), `ORD8338374164` (Normal 1 kontainer, `TRC34520886`), `ORD8338534214` (Multipickup), `ORD8338620381` (Multidrop). Skenario oms013 yang dulu blocked karena precondition ini (POS-023/024/025/026, EDG-011, NEG-007) bisa dites ulang. **EDG-007 butuh 3 kontainer — belum ada order Ditugaskan 3 kontainer**, tetap blocked kecuali user membuat + menugaskan order baru.
- **Edit Order pada status Ditugaskan diblokir di level route**: akses `/order/{uuid}/edit` → redirect ke `/order/{uuid}` + toast "Order ini tidak dapat diedit lagi." Menu Aksi Ditugaskan juga tanpa item "Edit". Expected result NEG-007 = perilaku ini.
- **"Waktu Perjalanan: 5 Hari" TAMPIL di Detail Order FCL Ditugaskan**, tidak tampil sama sekali di Menunggu Penugasan → separuh kedua EDG-011 kini bisa diverifikasi.
- **Popup "Data No. Perjalanan"**: bukan `role="dialog"`; tombol per baris aria-label **"Salin nomor"**; tombol tutup teks **"Tutup"**; baris = No. Perjalanan + `<No. Kontainer>•<Jenis Kontainer>`. Order lama pakai nomor kontainer manual (`AUTOTEST-20260829-CTN...•Long Boy`), order baru otomatis `CNT<kode order>•20 ft`.
- **Step 3 FCL**: default PPh sekarang **2%** (run 2026-08-24 mencatat 0% → POS-014 failed; re-test sebelum menutup temuan). Tidak ada checkbox "Simpan data ke master harga" (asumsi #17 desain 075/083/091/092 tidak ada di live). Helper Harga: "Mencakup seluruh biaya kontainer pada order ini".
- **Modal "Lihat Detail" Step 3 berjudul "Data Pengirim"** (bukan "Detail Multipickup"/"Detail Multidrop" seperti istilah dokumen skenario), bukan `role="dialog"`, tombol "Tutup". Sesuaikan assertion judul.
- **Modal "Pilih Barang"**: search placeholder "Cari Kode SKU atau Nama Barang", label "Sudah ditambahkan" di bawah nama item yang sudah dipilih, tombol "Tambahkan" + icon X aria "Tutup" (bukan Batal/Simpan), tidak ada counter angka terpilih.
- **Sub-modal "Pilih Jenis Kontainer"** menampilkan 11 jenis kontainer tanpa filter rute (EDG-012 bisa dites ulang). GOTCHA: sub-modal **tidak ikut tertutup** saat panel Hitung Ulang Kontainer ditutup via Escape — tutup manual via X "Tutup" sebelum lanjut agar selector berikutnya tidak nyasar.
- **Step 1 "Metode Pengiriman" WAJIB diklik eksplisit** walau kartu "Door to Door" tampak ter-highlight default — tanpa klik, "Selanjutnya" memunculkan "Metode Pengiriman harus dipilih".
- **Konfirmasi "Batal"** (wizard & Edit Order): heading "Apakah Anda yakin ingin membatalkan ?", teks "Data yang telah Anda masukkan akan hilang.", tombol `Tidak` / `Ya`. Klik "Ya" → redirect ke `/order` (wizard) atau Detail Order (edit), tanpa menyimpan.
- **"Order Kembali" muncul di KEDUA status** (Ditugaskan & Menunggu Penugasan) → FND-01/SCR-03 `selector-map-order.md` dikoreksi.
- **Console noise**: CORS error `GET /api/notifications/unread-count` saat `/order/buat` dibuka — jangan dihitung sebagai kegagalan skenario; laporkan terpisah ke dev.
- Panel filter `/order` live = 12 field, tanpa "Metode Pengiriman"/"Total Harga" (desain 069) — skenario filter yang mengandalkan 2 field itu → `blocked`/gap desain, bukan failed.

## 2026-09-05 (siang) — `/explore oms014-order-ftl-fcl-normal`: toggle Auto Stuffing tetap tidak ada, precondition FTL kini terisi, 0 data ditulis

Detail lengkap: `explore/module-map.md` section "Detail Layar Modul Order — Varian FTL Normal/Multi + status Auto Stuffing (oms014)" + Temuan Janggal #18–#26. Dijalankan via **Playwright lokal headless** (`artifacts/explore-scripts/lib.js`, `probe-*.js`) karena browser Playwright MCP dipegang sesi Claude lain yang masih hidup (bukan lock basi — jangan hapus `Singleton*`). Jalur ini sah untuk explore/probe; eksekusi modul resmi tetap via `scripts/run-playwright.sh` / MCP. Catatan yang MEMENGARUHI executor oms014 berikutnya:

- **Premis modul (Auto Stuffing OFF) tidak tercapai di staging** — toggle tidak ada di UI maupun API (2x: 2026-08-23 & 2026-09-05). Live: FAB `Hitung Ulang Armada` (disabled sampai ada barang) + `Visualisasi Terbaru` (enabled sejak awal) ADA di Step 2 semua tipe & di Edit Order, drawer rekomendasi berfungsi penuh; Step 4 & Detail Order hanya punya `Visualisasi Muatan` generik (render 3D normal, tanpa 404). **Skenario AS-OFF & toggle (POS-007/008/009/010, NEG-028, POS-070, EDG-022, STR-018, POS-057/058/059/060, NEG-020/021/041/042) → `blocked` merujuk Temuan #18, bukan `failed`, sampai user memutuskan** opsi (a) tenant/konfigurasi OFF dari dev, (b) re-run hanya rule standar, atau (c) revisi assertion jadi positif.
- **Data FTL tersedia** (total 167 order, 106 FTL): Ditugaskan 13 (`ORD8337312450` Multipoint → No. Perjalanan `TRC61607810`, `L9721IK • CDE`; `ORD8337252940` Multidrop; `ORD8337194621` Multipickup — vendor PT. Intan Emas Permata), Menunggu Penugasan 46 (`ORD8500349521` Normal, `ORD8495600328` Multidrop), Proses Pengiriman 3, Terkirim 17 (`ORD8508571221`), Dibatalkan 10 (`ORD8497566750`), draft-like 17 (`ORD8572707826` Isi Data Muatan). POS-046/EDG-031/POS-055/EDG-030/NEG-023/NEG-024/NEG-040 bisa dites.
- **Route draft "Lanjutkan Pengisian" = `/order/buat?id={uuid}`**, bukan `/order/{uuid}`; Detail/Edit tetap `/order/{uuid}` — ambil href dari menu Aksi.
- **Menu Aksi**: draft-like = Detail, Lanjutkan Pengisian, Batalkan Order, Riwayat Perubahan; Menunggu Penugasan = Detail, Edit, Batalkan Order, Order Kembali, Riwayat Perubahan; Ditugaskan = Detail, Lihat No. Perjalanan, Batalkan Order, Order Kembali, Riwayat Perubahan; Proses Pengiriman/Terkirim/Dibatalkan = Detail, Order Kembali, Riwayat Perubahan (tanpa Edit/Batalkan → FND-05 triage 08-23 kemungkinan fixed; NEG-022 re-test URL edit langsung). Item "Lihat No. Resi" dipakai untuk LCL.
- **Opsi filter Status persis**: Semua Status, Isi Data Pengiriman, Isi Data Muatan, Isi Data Vendor, Review Order, Menunggu Penugasan, Ditugaskan, Proses Pengiriman, Terkirim, Dibatalkan ("Isi Data Pengiriman", bukan "Isi Data Dasar").
- **Step 1**: kartu FTL sudah terpilih default; `Simpan ke Draf` & `Selanjutnya` enabled sejak awal (NEG-001/EDG-033 → gap desain). Company (`Semua Pengirim`/`Semua Penerima`) dipilih sebelum Drop Point; Drop Point Asal tampak TIDAK terfilter per company (kontra entri 2026-08-28 — verifikasi, jangan hard-fail). Jenis Armada 8 opsi: Trailer 40 Feet, Trailer 20 Feet, Tronton, Fuso, CDD Long, CDD, CDE, Pickup (Trailer 40 Feet = 35.000 kg / 75 m³).
- **Multi-alamat Step 1**: memilih tipe langsung merender blok "Pengirim 1/2" (Multipickup), "Penerima 1/2" (Multidrop), keduanya (Multipoint); blok 1-2 **tanpa tombol Hapus** → NEG-033/043/044 tidak bisa dieksekusi dari kondisi awal (`blocked`). `Tambah Baris Input` untuk blok ke-3+.
- **Step 2**: empty-state `Belum ada barang. Klik "Pilih Barang"` (tanpa spasi sebelum kutip — koreksi ASM-024); unit kosong memblokir dengan `Minimal 1 baris barang pada Armada N` (POS-041/EDG-017 tetap blocked); badge `Berat melebihi kapasitas armada` non-blocking; **Nomor DO dirender teks satu baris, bukan chip** (POS-024/025/EDG-006/STR-005 → gap desain); sub-section `Pick Up n - <alamat>` / `Drop Off n - <alamat>`, Multipoint = dua baris terpisah per kombinasi; `Tambahkan Asuransi` per unit.
- **Step 3**: `Waktu Perjalanan` tampil SEBELUM Vendor dipilih (kontra entri 2026-08-23 NEG-016/EDG-013 — verifikasi ulang saat run); PPN 11%, PPh 2%, Asuransi 0,2%.
- **Konfirmasi Batal** (wizard & Edit): `Apakah Anda yakin ingin membatalkan ?` / `Data yang telah Anda masukkan akan hilang.` / `Tidak` / `Ya`. Modal Batalkan Order: textarea `#cancelReason`, placeholder `Tuliskan alasan pembatalan order`.
- **Master Barang** 22 SKU, nama terpanjang 26 karakter → EDG-032 tetap blocked. Master Drop Point tidak punya kolom Kota (nested per perusahaan).
- **Login headless**: 2x timeout transient menunggu form login (bukan kredensial ditolak) — retry sekali wajar; tetap patuhi guard 2x gagal kredensial.

## 2026-09-06 — `/explore oms015-order-ltl-lcl-universal`: precondition Ditugaskan/Terkirim terisi, FND-OMS015-01/05/12 dikonfirmasi ulang MASIH REPRODUCE, gap ASM-035 RESOLVED

Detail lengkap: `explore/module-map.md` section "Detail Layar Modul Order — Varian LTL/LCL (oms015)" (10 screenshot `oms015-ltl-lcl-*`). Re-explore read-only via subagent oms-explorer (browser MCP), mengisi celah run `20260827-075803` (224 passed/29 failed/26 blocked, lihat `results/_triage__oms015-order-ltl-lcl-universal__20260827-075803.md`). Wizard LTL diisi sampai Step 3 lalu **Batal**; 0 data tersimpan (Kuota Order tetap 8/200).

- **Precondition data kini terisi (sebagian)**: LTL 21 order total (Ditugaskan 2: `ORD8337730685` PT. Intan Emas Permata, `ORD7886177064` PT. Wahana Logistik — 2 vendor beda, rute sama Surabaya→Bandung; Terkirim 1: `ORD7373883424`); LCL 6 order total (Ditugaskan 1: `ORD8416187200`; Terkirim 1: `ORD8338814778`, sebelumnya "Menunggu Penugasan" di entri 2026-09-02, kini sudah Terkirim — siklus end-to-end LCL terbukti jalan). 16 skenario blocked run 2026-08-27 karena 0 order lanjut kini punya precondition, meski sampel tipis (1-2 per status).
- **FND-OMS015-01 (Major/Critical, filter Drop Point oleh Kota, kontra REQ-009) — MASIH REPRODUCE**: baseline Drop Point Asal/Tujuan 26 opsi; Surabaya→Bandung → Asal 8 opsi, Tujuan 1 opsi (`EL - Droppoint Bandung Haier`); Medan→Palembang → **kedua dropdown kosong total**, order ke rute ini mustahil dibuat lewat UI. Konsisten dengan entri 2026-08-28.
- **FND-OMS015-05 (Major, checkbox "Gunakan komponen harga" default checked, kontra AC-029.3) — MASIH REPRODUCE**: Step 3 LTL Surabaya→Bandung, checkbox tercentang sejak render, PPN 11%/PPh 2% otomatis. Konsisten dengan entri 2026-08-28.
- **FND-OMS015-12 (Riwayat Pembatalan tanpa kolom Alasan Pembatalan) — MASIH REPRODUCE**: header tabel persis `ID Order` / `Vendor` / `Kota Asal` `Warehouse Asal` / `Kota Tujuan` `Warehouse Tujuan` / `Total Harga` `Status` / kolom aksi tanpa header — tidak ada elemen Alasan Pembatalan sama sekali.
- **GAP ASM-035 RESOLVED** (dulu: "No. Resi tidak ada di Detail Order"): Detail Order LTL/LCL kini punya section/accordion **"No. Resi"** tersendiri (setelah "Vendor dan Harga"), tabel identik popup (`No`/`No. Resi`+salin/`Kode SKU`/`Nama Barang`). Skenario REQ-058 bisa ditutup passed di run berikutnya, bukan gap desain lagi.
- **Koreksi selector popup "Data No. Resi"**: tombol tutup adalah **teks "Tutup"**, bukan ikon X seperti asumsi `ui-inventory` UI-104/105.
- Contoh No. Resi live: LTL `ORD8337730685` → `RES90584378`/SKU `IK-APT-001`; LCL `ORD8416187200` → `RES53860041`/SKU `IK-APT-001`.

## 2026-09-06 (lanjutan) — `/explore oms017-penugasan-tracking`: FND-01/02/11 FIXED, FND-10 tak terverifikasi (gap data), gap order CY-* masih ada, 2 kandidat bug baru, 0 data ditulis

Detail lengkap: `explore/module-map.md` section "Detail Layar Modul Penugasan Tracking (oms017)" (13 screenshot `oms017-penugasan-*`) + Temuan Janggal #27–#29. Re-explore read-only via subagent oms-explorer (browser MCP). Form Tambah/Edit Penugasan dibuka lalu keluar via Batal; tidak ada penugasan dibuat/diubah. Catatan yang MEMENGARUHI executor oms017 berikutnya:

- **FND-01 FIXED** (filter AND Sopir + Jenis Order): FTL + Loekman → 8 baris, bukan 0. TC-P-021 kandidat passed saat re-run.
- **FND-02 FIXED** (filter No. Polisi/No. Kontainer tidak mencari kontainer): `CNT8338814778` → 1 baris `ORD8338814778`. TC-E-009 kandidat passed.
- **FND-11 FIXED, tapi bentuknya beda dari ekspektasi skenario**: Edit Penugasan FCL status Dalam Perjalanan (`ORD8338721286`) tidak merender form sama sekali — pesan "Tidak Bisa Edit — Tidak bisa melakukan edit karena sudah dalam proses pengiriman." + tombol "Kembali ke List". Skenario TC-N-053/054/055, TC-E-039/040 yang meng-assert "field terkunci" perlu disesuaikan menjadi "halaman Edit ditolak" agar tidak false-fail.
- **FND-10 TIDAK DAPAT DIVERIFIKASI**: 25/25 penugasan yang dicek hanya punya 1 entri "Penugasan Awal" di panel Riwayat Penugasan dan "Belum ada riwayat perubahan" di `/penugasan-tracking/{uuid}/riwayat`. Butuh ≥1 penugasan yang pernah diedit (Belum Berangkat, via URL edit langsung) sebelum TC-P-062/TC-E-038 bisa di-re-run — tetap blocked, bukan fixed.
- **Struktur live yang BEDA dari dokumen skenario** (sumber false-fail potensial): (a) menu Aksi baris **hanya Detail + Riwayat Perubahan** untuk semua jenis/status — tidak ada Edit Penugasan / Batalkan Penugasan / Isi Kendala / Penugasan Sopir Bongkar (mockup 139), tidak ada perbedaan Trucking vs Container (mockup 128/129); (b) **Edit Penugasan hanya via URL** `/penugasan-tracking/{uuid}/edit`; (c) panel Filter **9 field** — ID Order, Jenis Order, Kota Asal, Kota Tujuan, No. Polisi / Kontainer, Sopir / Petugas, Status, **Tahapan Tracking**, **Nama Vendor** — tanpa field Tanggal; (d) "Riwayat Penugasan" di Detail = **panel inline** via tombol "Lihat Detail", bukan modal; (e) kolom tabel: ID Order/Vendor · Rute · No. Polisi/No. Kontainer/Sopir · Status · Aksi — tidak identik mockup 127/139/145.
- **Precondition**: pagination TERPENUHI (65 baris / 4 halaman) → TC-P-004/005, TC-N-006, TC-E-002/003 bisa di-re-run. Order metode **CY-*/Door-CY MASIH 0** (15 FCL/LCL di tabel semua Door to Door; Pilih Order di Tambah Penugasan = 20 FTL, 0 FCL/LCL) → TC-P-043/044/045/056, TC-N-033/042/043 tetap blocked. Role Shipper-scoped tetap blocked (akun #3 kosong).
- **Kandidat bug baru (belum ada FND, perlu triase)**: (1) entry point Edit Penugasan hilang di sisi Admin (Temuan #27) — by design atau regresi?; (2) banner "mode read-only" pada Edit FCL Belum Berangkat milik vendor **tidak ditegakkan di level field** (`disabled=false`/`readonly=false`/`pointer-events:auto`, Temuan #28) — belum dicoba submit; perlu uji apakah backend menolak.
- **Infra**: 2x `522 Connection timed out` Cloudflare ke staging dalam 1 sesi, retry langsung sukses (Temuan #29). Tambahkan retry ringan `page.goto` di executor; jangan tandai failed karena ini.
- Dialog Batal form Tambah Penugasan (sisi Admin): "Perubahan akan hilang. Yakin ingin membatalkan?" — teks beda dari dialog Batal wizard Order ("Apakah Anda yakin ingin membatalkan ?").

## 2026-09-06 (lanjutan 2) — `/explore oms022-public-tracking-improve`: 5 FND masih reproduce (FND-01 bukti non-seed), verifikasi 4 digit tidak bisa di-bypass, POS-030/EDG-017 terbuka, 3 koreksi selector-map, 0 data ditulis

Detail lengkap: `explore/module-map.md` section "Detail Layar Modul Public Tracking (oms022)" (14 screenshot `oms022-*`) + Temuan Janggal #30–#34 + blok "Update pasca-explore 2026-09-06" di `shared/selector-map-tracking.md`. Re-explore read-only via subagent oms-explorer (browser MCP); sesi admin hanya untuk membaca No. Perjalanan/Resi + 4 digit WA PIC Penerima. Kuota Order 8/200 sebelum & sesudah. Catatan yang MEMENGARUHI executor oms022 berikutnya:

- **FND-OMS022-01 MASIH REPRODUCE, caveat seeding melemah**: 3 order multi-alamat in-progress (`TRC37808714` FTL Multipoint, `TRC03546401` FCL Multipoint, `TRC62627090` FCL Multipickup — semua Proses Pengiriman, `On Delivery` reached) → 0 entri transit menuju bongkar; 2 di antaranya punya timestamp bervariasi (bukan seed seragam). Rekomendasi triase §2 ("verifikasi dgn order non-seed dulu") kini terpenuhi → eskalasi ke dev sebagai bug Major bersama FND-OMS022-02 (silent failure 500, re-confirmed via route mock).
- **FND-OMS022-03/04/05 masih reproduce** (Enter tokenize saja; Hapus chip tidak menghapus kartu; multi-resi menambah).
- **Koreksi selector-map 2026-09-02 (WAJIB diikuti)**: (a) modal verifikasi **auto-close** setelah 4 digit salah — skenario retry harus klik `Lihat Detail` ulang; (b) deep link `/tracking?resi=<no>` **auto-search on-load** (bukan sekadar isi input); (c) state awal punya section baru h2 `Cara Lacak Pengiriman` (3 langkah) — bukan elemen terlarang UI-T10; (d) modal verifikasi punya subjudul `No. Perjalanan: <no>`.
- **Keamanan OK**: `/tracking/{no}` tanpa verifikasi selalu redirect ke `/tracking?resi={no}` (kartu ringkas), termasuk tab baru pada context sama — 4 digit tidak bisa di-bypass.
- **Mapping status admin → publik**: Ditugaskan → `Pickup` (0 riwayat, UI-T08 empty-state `Belum ada riwayat pengiriman untuk nomor perjalanan ini.`), Proses Pengiriman → `On Delivery`, Terkirim → `Delivered`.
- **Halaman publik menerima No. Resi LTL/LCL** (`RES…`) — layar detail identik dengan FTL/FCL (label tetap "No. Perjalanan", tanpa breakdown per-SKU).
- **Precondition terbuka**: POS-030 via `ORD7556205186` (2 No. Perjalanan `TRC11182633`/`TRC90557595`, 4 digit 7802); EDG-017 via order LTL/LCL Dibatalkan `ORD8331456722`/`ORD8331533103` (resi ada di admin, publik "tidak ditemukan" — konfirmasi PO expected/tidak sebelum verdict); UI-T08 via order Ditugaskan mana pun. **Tetap blocked**: Grup A (14 SCN checkpoint parsial), Grup B (3 SCN seed/API), EDG-025.
- **Data uji**: tabel lengkap 19 nomor + 4 digit di module-map section oms022. Order multi-alamat punya >1 penerima → beberapa kandidat 4 digit; pakai yang sudah terbukti.
- **Kualitas data staging**: `TRC03546401` punya timestamp masa depan (pola cron mingguan) → jangan assert `timestamp ≤ now`; `TRC62627090` punya entri lokasi tak cocok order (Pematangsiantar).
- **Batas 10 nomor** ditegakkan diam-diam (nomor ke-11 hilang tanpa pesan) — Temuan #31.
- **Timing admin**: filter ID Order di `/order` butuh `networkidle` sebelum klik Aksi; wait pendek membuat klik jatuh ke baris teratas unfiltered. 1x 522 Cloudflare, retry sukses.

## 2026-09-06 (lanjutan 3) — Batch 5 oms017-penugasan-tracking (runId 20260906-101116): 7 passed, 6 blocked, konfirmasi definitif gap CY-*/2-kontainer FCL via API recon

Detail hasil: `results/_batch05__oms017-penugasan-tracking__20260906-101116.json`. Group B (Tambah Penugasan) lanjutan Batch 4, tetap via akun Admin (finance.roro1@gmail.com) — akun Vendor tidak punya order tersedia.

- **KONFIRMASI DEFINITIF via network recon (bukan sampling manual)**: endpoint `GET /api/orders?hasPenugasan=false&status=DRAFT,ASSIGNED,MENUNGGU_PENUGASAN` (dipakai layar "Pilih Order" Tambah Penugasan) di-intercept via `page.route()` + limit dinaikkan ke 100 sehingga SELURUH pool unassigned (47 order) tertangkap dalam 1 response: 27 FTL, 9 LTL, 3 LCL, 8 FCL. **Field kunci per order**: `serviceType`, `metodePengiriman` (enum `DOOR_TO_DOOR`/`CY_TO_DOOR`/`CY_TO_CY`/`DOOR_TO_CY`), `jumlahArmada` (dipakai generik utk jumlah armada MAUPUN jumlah kontainer). Hasil: **seluruh 8 FCL + 3 LCL bermetode DOOR_TO_DOOR, seluruh 8 FCL berjumlahArmada=1** — nol order CY-*/Door-CY, nol FCL 2-kontainer di pool saat ini. Ini menjelaskan blocked TC-P-041/043/044/045/046. Teknik recon ini (route-intercept limit tinggi) direkomendasikan utk executor berikutnya yang perlu screening precondition data secara pasti tanpa trial-and-error UI berulang — jauh lebih cepat & akurat daripada klik satu-satu.
- **Order 2-kontainer FCL yang pernah ada (ORD7556205186, ORD7466106555, run 20260829) sudah tidak unassigned lagi** — kemungkinan terkonsumsi task ad-hoc 2026-09-02/03. Perlu provisioning order FCL baru (Jumlah Kontainer=2, metode Door to Door) via modul Order bila ingin re-test TC-P-041/046 presisi; dan order CY-*/Door-CY belum pernah ada sama sekali di staging manapun sejak run pertama (2026-08-29) — kemungkinan besar tidak akan pernah tersedia tanpa provisioning eksplisit (form Buat Order kemungkinan tidak expose pilihan metode ini, perlu dicek terpisah).
- **GOTCHA KALENDER BARU (form Tambah Penugasan FCL sisi ADMIN, screen 134/135, beda dari vendor-portal 2026-09-03)**: cell tanggal bulan-lain (leading/trailing) memakai class Tailwind **token literal `text-gray-300`** (light mode), sedangkan cell bulan aktif memakai `text-gray-700 dark:text-gray-300` — regex substring `/text-gray-300/` akan **false-positive** mengecualikan cell bulan aktif juga (karena `dark:text-gray-300` mengandung substring yang sama). **Wajib** split `className` jadi token array (`.split(/\s+/)`) lalu cek `tokens.includes('text-gray-300')` utk exact match, bukan regex substring. Trigger tombol tanggal pakai teks placeholder literal `dd/mm/yyyy` (ETD/ETA, date-only) vs `dd/mm/yyyy hh:mm` (Closing Time, date+time) — `getByRole('button',{name:...})` WAJIB `exact:true` karena default substring match membuat kedua pola dianggap sama (nama pendek "cocok" dgn nama panjang).
- **Popup Closing Time (date+time) TIDAK tertutup oleh `Escape` maupun klik netral** — klik di pojok kiri-atas viewport (mis. `body.click({position:{x:5,y:5}})`) untuk "menutup dgn klik di luar" berisiko TINGGI mengenai link sidebar/logo dan **memicu navigasi tak sengaja yang me-reset seluruh form** (kejadian nyata di run ini, kehilangan seluruh isian & harus restart). Cara aman: klik ULANG tombol trigger yang sama (toggle close), atau klik elemen teks statis non-link yang diketahui aman (mis. judul fieldset "Data Kapal Connecting").
- **Fieldset "Detail Kapal Utama" TETAP WAJIB diisi meski Jenis Jadwal Kapal = Connecting** (bukan digantikan oleh Data Kapal Connecting) — konsisten dgn catatan 2026-09-03 sisi vendor-portal, kini terkonfirmasi juga sisi admin.
- **Dialog konfirmasi**: heading "Konfirmasi Penugasan", teks "Pastikan data penugasan sudah benar sebelum melanjutkan", tombol "Periksa Kembali" / "Simpan" (pola 2x tombol "Simpan" — `.first()` form, `.last()` dialog — tetap berlaku utk FTL maupun FCL, sisi admin).
- **Master Pelayaran terisi** (MERATUS, AUTOTEST20260829PELAYARAN) — blocker "Tidak ada pelayaran" dari run 20260829 SUDAH RESOLVED, tidak lagi jadi hambatan utk skenario FCL Direct/Connecting manapun di run ini.
- **Order dikonsumsi (assigned) Batch 5**: `ORD8500349516`, `ORD7798037664` (Fuso, master armada/sopir), `ORD8500349489`, `ORD7890180801` (FCL Multipoint, Connecting 2 baris). **Order dilihat tapi TETAP unassigned** (tersedia Batch 6-13): `ORD8500349463` (dipakai TC-N-018, view-only). **Order tersedia sisa** (dari 47 unassigned, minus yang dikonsumsi Batch4+5): mayoritas FTL jayaswastikaqw (ORD8500349505/488/479/464/457/351/292/264/266/263/232/182/174/145, ORD8500246591), `ORD8491214259` (jenis CDE x2 armada), FTL PT. Wahana Logistik (ORD7888731386/7880254292/7880104602/7880011345), FTL Fuso `ORD7713556226`, FTL Fusion (`ORD7285852191`/`ORD7285719128`), LTL `ORD7890736370`, LCL (`ORD7890583351`/`ORD7886330437`/`ORD7813655981`), FCL sisa (`ORD7890024647`/`ORD7889888868`/`ORD7889608568`/`ORD7881523690`/`ORD7881405350`/`ORD7881288656`/`ORD7881082637` — semua 1 kontainer Door to Door).

## 2026-09-06 (lanjutan 4) — Batch 6 oms017-penugasan-tracking (runId 20260906-101116): 11 passed, 2 blocked — validasi required field

Detail hasil: `results/_batch06__oms017-penugasan-tracking__20260906-101116.json`. Semua via akun Admin. **Tidak ada order yang ter-assign/tersimpan** di batch ini (13 skenario murni negative/blocked-required-field) — 8 order dipakai berulang untuk trigger validasi, SEMUA diverifikasi ulang tetap unassigned di akhir batch: `ORD8500349505`, `ORD8500349457`, `ORD8500349488`, `ORD8500349479`, `ORD8500349464` (FTL), `ORD7889608568`, `ORD7889888868`, `ORD7881523690`, `ORD7881405350` (FCL).

- **GOTCHA BARU KRITIS — dialog native `confirm()` (tombol Batal)**: mendaftarkan `page.once('dialog', async d => await d.dismiss())` SECARA EKSPLISIT di dalam skrip `browser_run_code_unsafe` menghasilkan perilaku SALAH (form/order ter-reset total, seolah-olah dialog di-*accept*, bukan di-dismiss). Solusi yang TERBUKTI BENAR: JANGAN daftarkan listener dialog sama sekali di dalam skrip — cukup trigger klik tombol Batal (dispatchEvent, boleh fire-and-forget/tanpa listener), biarkan tool Playwright MCP melaporkan "Modal state" pending, lalu biarkan Playwright **auto-dismiss default** (perilaku bawaan Playwright: dialog tanpa listener otomatis di-dismiss/Cancel) menyelesaikannya — baru query state halaman di call terpisah berikutnya. Alternatif eksplisit: panggil tool terpisah `browser_handle_dialog({accept:false})` SEGERA setelah tool run_code_unsafe melaporkan modal state (tapi race sering membuatnya "no modal state" krn auto-dismiss sudah lebih cepat — tidak masalah, hasil akhirnya sama benar). Ini **memperbarui/mengoreksi** catatan run 20260829 yang menyebut "dialog ter-dismiss otomatis, setara Tidak" — kini terbukti detail pentingnya adalah TIDAK BOLEH ada listener manual bersaing dengan auto-dismiss.
- **GOTCHA BARU — chaining banyak skenario negative pada 1 instance form tanpa reload TIDAK RELIABLE**: pola "isi field A kosong → Simpan → cek error A → perbaiki A, kosongkan field B → Simpan lagi → cek error B" pada order/form YANG SAMA (tanpa reload) beberapa kali GAGAL mendeteksi error kedua/ketiga (elemen error tidak muncul di re-check meski behavior aplikasi kemungkinan besar tetap benar) — kemungkinan besar isu re-validasi/touched-state React di sisi test, BUKAN bug aplikasi (dikonfirmasi dgn mengulang skenario yang sama persis pakai order/form SEGAR → error langsung terdeteksi normal). **Wajib untuk executor Batch 7-13**: gunakan order/halaman baru (reload penuh) per sub-skenario validasi required-field yang berurutan, JANGAN chaining banyak fill-simpan-fill-simpan pada 1 instance form yang sama tanpa reload di antaranya — chaining hanya aman untuk 1-2 langkah pertama (mis. isi field A kosong lalu langsung cek, tanpa memperbaiki-lalu-mengosongkan-field-lain sesudahnya).
- **TEMUAN BARU (observasi, bukan failed) — TC-N-019/D-07**: border input TIDAK PERNAH berubah warna saat validasi error (dicek via `getComputedStyle().borderColor` sebelum/sesudah error: tetap abu-abu standar `rgb(208,213,221)`/`border-gray-300`, tidak ada class atau computed style error/merah sama sekali) — hanya helper text error yang tampil di bawah field. Sesuai keputusan D-07 di analysis.md ("tidak ada mockup state error", assertion difokuskan ke keberadaan error + blokir simpan), TIDAK ditandai failed, tapi dicatat sebagai kandidat gap-desain/bug-candidate minor untuk triase akhir modul (styling error state field belum diimplementasikan visual, cuma teks).
- **Master armada & sopir KOSONG TOTAL ("Tidak ada armada") untuk SEMUA vendor yang order-nya masih unassigned di pool saat ini** (dicek eksplisit 4 vendor FTL: jayaswastikaqw, fdsa, PT. Wahana Logistik, PT. Solutiva Bronze — juga FCL PT. Wahana Logistik) — bukan cuma sebagian seperti diduga sebelumnya. PT. Jaya Karsa (IK) (vendor dgn master armada lintas-jenis lengkap) sudah HABIS order unassigned-nya. Konsekuensi: **TC-N-022** (butuh order+master lintas-jenis) blocked — precondition tidak bisa direproduksi dgn data pool saat ini sama sekali (bukan cuma "tidak lintas-jenis", tapi kosong total). **TC-N-030** (Jenis Jadwal Kapal belum dipilih) tetap blocked, konsisten run 20260829 — tombol "Direct" selalu default aktif begitu order FCL dipilih, tidak ada cara UI untuk deselect.
- **Placeholder FCL Sopir Muat dikonfirmasi ulang**: `Nama Sopir` (exact, TANPA prefix "Masukkan"), berbeda dari FTL yg pakai "Masukkan Nama Sopir" — perlu `{exact:true}` saat pakai `getByPlaceholder('Nama Sopir')` agar tidak collide dgn placeholder FTL di halaman lain.

## 2026-09-06 (lanjutan 5) — Batch 7 oms017-penugasan-tracking (runId 20260906-101116): 11 passed, 1 failed, 1 blocked — BATCH TERAKHIR GROUP B, wrap-up data utk GROUP C

Detail hasil: `results/_batch07__oms017-penugasan-tracking__20260906-101116.json`. Semua via akun Admin. `TC-N-033` langsung blocked sesuai arahan plan (gap CY-* dikonfirmasi definitif Batch 5, diverifikasi ulang awal Batch 7 — tetap 0 order CY-* dari 43 unassigned). `TC-E-013` **failed** — reproduksi bug lama **FND-15** (bukan temuan baru): Ringkasan Order order multipickup ORD7889888868 menampilkan `Kabupaten Sumenep` (bukan `Kab. Sumenep`), melanggar format REQ-028 literal meski konsisten di seluruh sistem — tetap ditandai failed sesuai kebijakan proyek (assert ke REQ, bukan "sesuai desain aplikasi"). Order FCL/LCL 2+ kontainer tetap tidak tersedia di pool (dipakai substitusi 1-kontainer utk TC-E-015/017/023, dicatat sbg gap parsial per skenario).

- **KOREKSI catatan D-07 lama**: analysis.md menyebut "tidak ada desain pesan error" utk ETD Connecting > ETA, tapi live app **SUDAH** menampilkan pesan inline persis `ETD Connecting maksimal Tiba (ETA)` tepat di bawah field (dibuktikan TC-N-032 & TC-E-021) — kemungkinan sudah di-fix sejak analysis ditulis atau analysis tidak lengkap. Bukan gap aplikasi lagi, hanya gap dokumentasi analysis.md.
- **KOREKSI asumsi A-13**: validasi ETD per-baris connecting **bekerja benar** juga pada baris ke-3 dari 3 baris (TC-E-021) — error muncul TEPAT di baris yang melanggar (baris 3), baris 1-2 valid tidak menampilkan error, Simpan tetap terblokir. Tidak ada bug di sini.
- **Temuan teknis baru — varian tampilan Jenis Jadwal Kapal lebih luas dari dugaan**: order metode **Door to Door** (bukan cuma Door-CY/CY seperti dicatat run 20260829) kadang tetap merender radio-card (`<button>` biasa, BUKAN `role=radio`) untuk Direct/Connecting — selector generik `getByRole('button', {name:/Connecting/})` sbg fallback WAJIB dipakai di semua screen (134-137), bukan hanya 136/137.
- **Fix teknis kalender — menutup popup Closing Time (date+time)**: klik elemen teks statis (mis. heading fieldset) via `dispatchEvent('click')` TERBUKTI TIDAK RELIABLE (bisa merusak state field lain). Cara benar: simpan `elementHandle()` tombol trigger SEBELUM memilih tanggal, lalu `dispatchEvent('click')` ULANG pada handle yang SAMA (bukan locator baru berbasis nama tombol, karena nama/teks tombol berubah setelah tanggal terisi).

### WRAP-UP GROUP B (Batch 4-7) — Order ter-assign "Belum Berangkat" tersedia utk GROUP C (Batch 8-11, Isi Data Tracking)

Total **10 order berhasil ditugaskan** sepanjang Group B (Batch 6 tidak menghasilkan penugasan baru — murni skenario negative/validasi):

| Order | Jenis | Batch/Skenario | Catatan |
|---|---|---|---|
| ORD8500349521 | FTL | Batch4 TC-P-023 | jayaswastikaqw, Isi Data Manual |
| ORD8500349517 | FTL | Batch4 TC-P-026 | Isi Data Manual |
| ORD8500349511 | FTL | Batch4 TC-P-035 | Isi Data Manual, No. WhatsApp dikosongkan (opsional) |
| ORD8500349516 | FTL | Batch5 TC-P-038 | jayaswastikaqw, Isi Data Manual |
| ORD7798037664 | FTL | Batch5 TC-P-039 | vendor "fdsa", jenis armada Fuso, dari **Master** (bukan manual) |
| ORD8500349489 | FTL | Batch5 TC-P-040 | Isi Data Manual, No. WhatsApp diisi |
| ORD7890180801 | FCL | Batch5 TC-P-042 | Multipoint, Door to Door 1 kontainer, Jadwal Connecting 2 baris |
| ORD8500349263 | FTL | Batch7 TC-E-018 | Isi Data Manual, data manual berkarakter spesial/spasi (AUTOTEST-20260906-*) |
| ORD8500349232 | FTL | Batch7 TC-E-019 | Isi Data Manual, No. WhatsApp format internasional |
| ORD7881082637 | FCL | Batch7 TC-E-020 | PT. Wahana Logistik, Door to Door 1 kontainer, Jadwal Connecting ETD=ETA |

Tidak ada order **LTL** atau **LCL** yang ter-assign selama Group B (LTL `ORD7890736370` & 3 order LCL hanya dilihat/dipakai utk edge-case tanpa Simpan final, tetap unassigned) — **GAP untuk GROUP C**: Batch 8-11 butuh minimal 1 penugasan LTL & LCL berstatus Belum Berangkat; kalau butuh presisi jenis LTL/LCL, executor Batch 8 perlu membuat penugasan baru dulu dari order LTL/LCL unassigned yang tersisa (`ORD7890736370` LTL; `ORD7890583351`/`ORD7886330437`/`ORD7813655981` LCL — status unassigned per akhir Batch 7, perlu verifikasi ulang via intercept API krn bisa berubah).

## 2026-09-06 (lanjutan 6) — Batch 8 oms017-penugasan-tracking (runId 20260906-101116): 11 passed, 1 failed — ENTRY POINT & STRUKTUR HALAMAN ISI DATA TRACKING DITEMUKAN (D-17 tidak lagi buta)

Detail hasil: `results/_batch08__oms017-penugasan-tracking__20260906-101116.json`. Semua via akun Admin. Canary (`TC-P-003`, `TC-P-047`) LOLOS di awal, tidak ada kill-switch yang terpicu — halaman Isi Data Tracking ternyata rapi & mostly sesuai REQ meski D-17 (tanpa mockup). **Ini info PALING KRUSIAL untuk Batch 9-11**, ganti seluruh asumsi `selectorHint` di scenarios.json (semua `data-testid` di dokumen TIDAK ADA di implementasi nyata):

### Entry point (menggantikan asumsi `menu-isi-data-tracking`/`form-tracking`)
1. Dari `/penugasan-tracking`, cari baris order. Klik `button[title="Aksi"]` (ikon lucide titik-tiga) — **WAJIB** `locator.evaluate(el => el.click())` (native DOM click); `locator.click()` maupun `dispatchEvent('click')` biasa TIDAK memicu handler React sama sekali (konsisten dgn catatan Batch 1).
2. Menu muncul sbg **portal** `div[class*="fixed"]` (BUKAN `role=menu`/`menuitem`). Isi menu **vendor-dependent**: vendor PT. Wahana Logistik & jayaswastikaqw (order2 run ini) → 6 item (`Detail`, `Edit`, `Isi Data Tracking`, `Isi Kendala`, `Alihkan Armada`, `Riwayat Perubahan`); vendor Intan Emas Permata/Jaya Karsa/SPK → hanya 2 item (`Detail`, `Riwayat Perubahan`, TIDAK ADA Isi Data Tracking — lihat Batch 1 utk detail kandidat bug REQ-050/007).
3. Klik teks `Isi Data Tracking` (exact match, native `el.click()` jg) → navigasi **butuh ~1.5-2 detik** (jangan langsung query DOM) ke **`/penugasan-tracking/<uuid>/isi-tracking`** (UUID, bukan kode ORD — konsisten pola proyek).

### Struktur halaman nyata (bukan asumsi form-tracking flat)
- Header: breadcrumb `Beranda > Penugasan Tracking > Isi Data Tracking`, tombol **`Kembali`** (button biasa, styling outline brand).
- Card info **7 item read-only** (REQ-053 100% sesuai): ID Order, Jenis Order, Rute, `Tanggal permintaan muat: ...`, Nopol, Jenis Armada/Kontainer, Sopir — semua `<span>` teks, 0 elemen input/select/textarea di card ini.
- Konten utama dipecah per **KOTA** (bukan langsung per alamat): tiap kota py pill collapsed `<Nama Kota> (N alamat)` bila sudah/belum aktif, atau expanded dgn `Saat ini X/Y alamat selesai` + `Pilih Alamat *` (list/card custom, **BUKAN** native `<select>` — selectorHint `select-alamat-tracking` TIDAK sesuai implementasi) bila sedang diproses.
- **TEMUAN PENTING (bugCandidate baru, lihat `TC-P-048` di hasil Batch 8)**: tiap alamat py widget **"Progres" 2 SUB-TAHAP**, bukan langsung form Selesai Muat/Bongkar: **(1) "Berangkat ke Alamat"** (field `Tanggal Berangkat ke Alamat *` + tombol `Simpan` SENDIRI, toast `Event tercatat` independen) **WAJIB** diisi & disimpan dulu → auto-collapse jadi ringkasan (`Berangkat Muat • <tgl>, Web, PIC: ...`) → baru **(2) "Selesai Muat"/"Selesai Bongkar"** terbuka (field tanggal + **Foto \*** upload + **Keterangan** opsional + `Simpan`). Ini KONTRADIKSI literal REQ-051 ("hanya 2 tahap, tidak ada tahap input lain") — reproducible di 4 order (FTL/FCL/LTL, sisi Muat maupun Bongkar). Sebelumnya D-06 hanya menduga ini muncul di History Tracking (read-only); ternyata ini FORM INPUT aktif.
- **FCL khusus**: setelah sisi Muat (semua kota) selesai, muncul section **`Kapal Berlayar` / `Kapal Sandar` / `Kapal Connecting`** (per-penugasan, bukan per-kota) dgn gating eksplisit: teks `"Selesaikan Muat dulu sebelum bisa isi Kapal Berlayar."` dan `"Isi Kapal Berlayar dulu sebelum bisa isi Kapal Sandar."` — mengonfirmasi rantai MUAT→KAPAL(Berlayar wajib sebelum Sandar)→BONGKAR yg disebut run sebelumnya.
- Sisi Bongkar diberi heading **`Penugasan Sopir Bongkar`** diikuti pill per kota drop (mis. utk order 3-kota-drop: `Kabupaten Banjar (1 alamat)`, `Kabupaten Gresik (1 alamat)`, `Kota Banjarmasin (1 alamat)`) — **REQ-054 (form per kota drop) TERBUKTI BENAR**, tiap kota = section terpisah berjudul nama kota persis.
- FCL/LCL tahap Selesai Muat menambahkan field **`No. Kontainer *` / `No. Segel *`** (editable, prefilled dari data Tambah Penugasan) sesuai REQ-063 — dikonfirmasi tersimpan & tampil baru di list setelah Simpan.
- Simpan Selesai Muat/Bongkar terakhir pada order → body menampilkan pesan **`"Semua alamat selesai — pengiriman SELESAI."`**, tiap kota berlabel `(1/1 alamat selesai)` + link `Lihat detail`; badge status di `/penugasan-tracking` berubah `Dalam Perjalanan` (setelah Selesai Muat pertama) → `Selesai` (setelah Selesai Bongkar terakhir) — REQ-021/022/052 terverifikasi persis.

### Gotcha teknis baru (WAJIB utk Batch 9-11)
- **Tombol `Simpan` (semua sub-tahap) & tombol `Kembali` TIDAK merespons `locator.click()` standar** — harus `page.evaluate(() => btn.click())` (cari via `Array.from(document.querySelectorAll('button')).find(b=>b.textContent.trim()==='Simpan')`), sama seperti tombol Aksi.
- **Datepicker custom** (trigger `getByRole('button',{name:'DD/MM/YYYY HH:mm',exact:true})`): popup berisi grid hari (42 sel) + **jam 00-23 (24 tombol)** lalu **menit 00-59 (60 tombol)** — 2 daftar terpisah standar (BUKAN pola "84 flat" yang dicatat run 20260829). **GOTCHA KRITIS BARU**: klik hari+jam+menit **TIDAK BOLEH** dalam satu `page.evaluate()` sinkron tanpa jeda — React belum commit state klik pertama sebelum klik berikutnya jalan, menghasilkan nilai salah (mis. jam ke-reset 00 walau sudah "diklik" 08). **WAJIB** pisah jadi `evaluate()` terpisah per elemen (hari → await/jeda → jam → await/jeda → menit), gunakan native `el.click()` di tiap evaluate (bukan Playwright `.click()` pada ElementHandle lama yg bisa stale). Tutup popup dgn `dispatchEvent('click')` pada tombol trigger yg kini bertuliskan tanggal terisi (exact text match).
- Upload foto: `input[type="file"]` standar, `.setInputFiles([...])` berfungsi, thumbnail muncul sbg `img[src^="blob:"]`.
- Sempat 522 Cloudflare + CORS `net::ERR_FAILED` ke `apioms-staging.prahu-hub.com` (token-refresh) saat pindah order beruntun cepat — solusi konsisten gotcha lama: `page.context().clearCookies()` + reload + login ulang, BUKAN indikasi bug aplikasi.

### Order yang dipakai/status akhir Batch 8
| Order | Jenis | Aksi Batch 8 | Status akhir |
|---|---|---|---|
| ORD8500349521 | FTL | TC-P-003/047/048/024/025 end-to-end penuh | **Selesai** (habis, jangan dipakai lagi) |
| ORD8500349517 | FTL | TC-P-050/051/052/053/054 (Selesai Muat sisi Muat saja) | **Dalam Perjalanan** |
| ORD7881082637 | FCL | TC-P-055 (Selesai Muat + ubah No. Kontainer/Segel) | **Dalam Perjalanan** |
| ORD7890180801 | FCL Multipoint | TC-P-049 (view-only, tidak ada data disimpan) | **Belum Berangkat** (cadangan) |
| ORD7890736370 | LTL | Provisioning awal batch (Tambah Penugasan, Isi Data Manual) + 1 sub-tahap "Berangkat ke Alamat" dicoba | **Belum Berangkat** (cadangan) |
| ORD7890583351 | LCL | Provisioning awal batch (Tambah Penugasan, Isi Data Manual + Jadwal Kapal Direct) | **Belum Berangkat** (cadangan) |
| ORD8500349511/489/263/232, ORD7798037664 | FTL | Tidak disentuh | **Belum Berangkat** (cadangan, 5 order) |

Total tersisa "Belum Berangkat": 5 FTL + 1 FCL (Multipoint) + 1 LTL + 1 LCL — jauh di atas minimum yang diminta plan (3 FTL + 1 FCL). "Dalam Perjalanan": 2 order (1 FTL + 1 FCL) tersedia utk Batch 9-13 (Edit per status, dsb).

### Provisioning LTL/LCL (sebelum skenario, sesuai arahan)
Diverifikasi ulang via intercept `GET /api/orders?hasPenugasan=false` di halaman `/penugasan-tracking/tambah` sebelum eksekusi — `ORD7890736370` (LTL) & `ORD7890583351` (LCL) TERKONFIRMASI masih unassigned, keduanya vendor **PT. Wahana Logistik** (bukan Intan Emas Permata seperti dugaan awal — jadi otomatis dapat menu 6-item Aksi). LTL: Isi Data Manual Nopol `AUTOTEST-20260906-B9001XY` / Sopir `AUTOTEST-20260906-Sopir-LTL1`, dialog "Konfirmasi Penugasan" → `Simpan` (`.last()`) → sukses. LCL (FCL-style, py Jenis Jadwal Kapal): No. Kontainer `AUTOTEST-20260906-CTN0001` / No. Segel `AUTOTEST-20260906-SGL0001`, Isi Data Manual Nopol `AUTOTEST-20260906-B9002XY` / Sopir `AUTOTEST-20260906-Sopir-LCL1`, Pelayaran MERATUS, Nama Kapal/Voyage `AUTOTEST-20260906-KAPAL1`/`AUTOTEST-VY001`, Closing Time `10/09/2026 08:00`, ETD `12/09/2026`, ETA `13/09/2026` — **gotcha lama terkonfirmasi ulang**: tombol radio "Direct" (Jenis Jadwal Kapal) harus diklik EKSPLISIT meski tampak ter-highlight default, kalau tidak Simpan silent-fail (tidak ada dialog konfirmasi muncul, tetap di halaman yg sama tanpa pesan error terlihat).

## 2026-09-06 (lanjutan 7) — Batch 9 oms017-penugasan-tracking (runId 20260906-101116): 5 passed, 4 failed, 3 blocked — form Penugasan Sopir Bongkar & dropzone upload dipetakan, blocker data sistemik ditemukan

Detail hasil: `results/_batch09__oms017-penugasan-tracking__20260906-101116.json`. Semua via akun Admin (finance.roro1@gmail.com). GROUP C lanjutan (Batch 9/4).

### Gotcha baru — tombol Login juga butuh native click
`locator.click()` standar pada tombol "Login" di `/login` TIDAK memicu handler React (form tidak submit, tetap di halaman login tanpa pesan error apapun) — gejala identik dgn tombol Aksi/Simpan/Kembali yg sudah didokumentasikan sebelumnya. **Wajib** `locator.evaluate(el => el.click())` juga untuk tombol Login. Ditemukan saat sesi sempat ter-logout akibat CORS/522 (gotcha lama) dan re-login gagal 2x dgn `.click()` biasa sebelum beralih ke native click yang langsung berhasil — BUKAN kegagalan kredensial.

### Provisioning 2 FCL (awal batch, sesuai arahan)
Verifikasi pool via route-intercept `page.route()` (rewrite `limit=` di URL request asli, bukan `page.evaluate(fetch())` yg gagal 401 krn token disimpan di luar localStorage/cookie yg bisa dibaca `fetch` biasa — WAJIB pakai `route.continue({url})` dgn header request ASLI) → dari 38 unassigned, 6 FCL kandidat SEMUA masih tersedia: `ORD7890024647`, `ORD7889888868`, `ORD7889608568`, `ORD7881523690`, `ORD7881405350`, `ORD7881288656` (semua vendor PT. Wahana Logistik, Door to Door, 1 kontainer).

Ditugaskan 2: **`ORD7890024647`** (Multidrop 3 kota: Kota Balikpapan→Kota Balikpapan/Kab. Sumenep/Kab. Banjar) dan **`ORD7889608568`** (Kota Balikpapan→Kab. Sumenep) — keduanya via Isi Data Manual (No. Kontainer/No. Segel/No. Polisi/Nama Sopir prefix `AUTOTEST-20260906-`), Mode Penugasan **"Tugaskan ke Pengurus"** (field baru dibanding catatan Batch 4-7, muncul sbg radio-card ke-3 setelah Armada/Sopir Muat — pilihan lain "Tugaskan ke Sopir"), Jenis Jadwal Kapal Direct (klik eksplisit via `getByText` fallback krn `getByRole('button',{name:'Direct'})` tidak match — render sbg div bukan button di form ini), Pelayaran MERATUS, dialog "Konfirmasi Penugasan" → `Simpan` `.last()` → sukses. Keduanya **tetap "Belum Berangkat"** — cadangan FCL utk Batch 13 (Edit per status). **CATATAN PENTING**: `ORD7889608568` terpakai jugai utk TC-P-057/058 (Penugasan Sopir Bongkar, Simpan tidak berhasil — lihat bawah), jadi order ini punya sedikit data tambahan (Nopol Bongkar manual sempat diketik tapi TIDAK tersimpan krn Sopir Bongkar tidak bisa diisi) — `ORD7890024647` tetap 100% bersih utk Batch 13.

### Entry point Penugasan Sopir Bongkar — 2 jalur terkonfirmasi, KEDUANYA ke `/tugaskan-bongkar` yang sama
1. **Action menu daftar** (`button[title="Aksi"]` di `/penugasan-tracking`) → item "Penugasan Sopir Bongkar" (5-6 item menu, vendor-dependent) → langsung ke `/penugasan-tracking/<uuid>/tugaskan-bongkar`, TIDAK PERLU status order sudah lanjut (tersedia bahkan saat order masih **"Belum Berangkat"**, dikonfirmasi eksplisit — bukan hanya saat "Dalam Perjalanan" seperti dugaan sebelumnya).
2. **Tombol inline** di dalam halaman `/isi-tracking`, section **BONGKAR**: heading "Penugasan Sopir Bongkar" diikuti tombol **asli** `<button class="rounded-lg border-brand-300 ...">Penugasan Sopir Bongkar</button>` (cursor:pointer, TIDAK disabled) — muncul persis di atas pill kota-kota drop, **tersedia sejak awal** (bahkan sebelum Muat dimulai sama sekali, dikonfirmasi pada `ORD7889608568` yang masih 0/1 alamat Muat). Klik tombol ini → navigasi ke URL `/tugaskan-bongkar` **yang SAMA PERSIS** dgn jalur 1 (bukan halaman terpisah).

**Koreksi D-10 & catatan run 20260829**: sebelumnya diasumsikan "Penugasan Sopir Bongkar" muncul "pada saat update Selesai Bongkar" (REQ-064 literal) — ternyata TIDAK, ini murni tombol/menu independen yang tidak terikat gating Muat→Kapal→Bongkar sama sekali. **PELAJARAN untuk Batch 10-13**: jika perlu menguji sopir bongkar pada order tertentu, cek jalur ini SEBELUM memajukan order ke status "Selesai" — begitu order "Selesai", KEDUA entry point hilang total dari menu/halaman (dikonfirmasi pada `ORD7881082637` yg tidak sengaja diselesaikan penuh Batch 9 ini sebelum sempat diuji sopir bongkar-nya, sehingga TC-P-057/058 harus pindah ke `ORD7889608568`).

### Struktur form `/tugaskan-bongkar` (real, bukan asumsi scenarios.json)
Header info read-only: ID Order, Jenis Pengiriman, Pelabuhan Asal/Tujuan, Jenis Kontainer, Metode Pengiriman, **Tiba (ETA)**, No. Kontainer, No. Segel, Mode Penugasan (read-only, mengikuti pilihan awal Tambah Penugasan). Field input:
- **Tanggal Permintaan Bongkar \*** — datepicker `DD/MM/YYYY hh:mm`, ada helper teks **"Tidak boleh sebelum ETA (…)"** (validasi presisi, di luar REQ-064 literal — konsisten D-09).
- **Armada Bongkar \*** — radio **"Pilih Dari Master" / "Isi Data Manual"** (dua-duanya enabled, TIDAK redup/disabled — mengoreksi catatan run 20260829 yg menyebut "class dimmed" sbg observasi visual saja, kini terverifikasi ulang benar-benar clickable). Isi Data Manual → input teks "No. Polisi" (placeholder "Masukkan No. Polisi").
- **Sopir Bongkar \*** — **TIDAK ADA radio/mode manual sama sekali** (beda dari Armada), hanya SATU combobox master `button[aria-haspopup]` teks "Sopir/No. WhatsApp" dgn search "Cari Sopir/No. WhatsApp" — **kontradiksi tegas REQ-065** ("Sama dengan REQ-031 & REQ-032", yg keduanya dual-method). Ini REPRODUCE PERSIS temuan run 20260829, dikonfirmasi ulang independen.
- Tombol **Batal / Simpan**.

**BLOKER DATA SISTEMIK BARU (penting utk Batch 10-13)**: satu-satunya vendor FCL yang order-nya masih bisa diakses lewat Isi Data Tracking/Penugasan Sopir Bongkar (6-item action menu) adalah **PT. Wahana Logistik**, dan **Master Sopir vendor ini KOSONG TOTAL** (`/master/sopir`: baris "PT. Wahana Logistik" = "Jumlah Sopir: -"). Vendor lain yg Master Sopir-nya TERISI (PT. Jaya Karsa 1 sopir "Mayora", SPK 2 sopir, PT. Solutiva Bronze 5 sopir) SEMUA order-nya dibatasi ke **2-item action menu** (Detail + Riwayat Perubahan saja, TIDAK ADA Isi Data Tracking/Penugasan Sopir Bongkar — dikonfirmasi ulang eksplisit pada `ORD7466529895` milik Jaya Karsa yang notabene order yg SAMA dipakai sukses pada run 20260829 — kemungkinan scoping vendor berubah/diperketat sejak saat itu). **Konsekuensi**: `TC-P-056`/`TC-P-057` **tidak bisa mencapai "notifikasi tersimpan" dgn data staging manapun saat ini**, terlepas metode manual/master — bukan sekadar gap satu order, tapi kombinasi 2 gap (UI: sopir bongkar no-manual; data: vendor-scoping) yang saling memperkuat. Rekomendasi utk executor berikutnya/BA: (a) tambah Master Sopir utk PT. Wahana Logistik, ATAU (b) perbaiki vendor-scoping action menu supaya vendor ber-Master-Sopir bisa akses Isi Data Tracking.

### REQ-066 (Batal sopir bongkar) — FND-03/06/08 REPRODUCE, kini pada KEDUA entry point
Diuji entry point **inline** (tombol dalam Isi Data Tracking, bukan action menu — beda dari run 20260829 yg mengetes action-menu) pada `ORD7889608568`: klik Batal → **langsung** ke `/penugasan-tracking` (daftar), **TIDAK ADA dialog konfirmasi sama sekali** (berbeda dari run 20260829 yg masih sempat menampilkan dialog custom "Ya/Tidak" sebelum redirect ke list) — kemungkinan `router.push('/penugasan-tracking')` di-hardcode pada tombol Batal, tidak membedakan asal navigasi maupun state form. REQ-066 (kritikal, ditekankan berulang di spec) tetap **gagal** pada kedua entry point yang pernah diuji lintas-run.

### Temuan form negatif Isi Data Tracking (order FTL `ORD8500349511`)
- **Info order read-only (TC-N-039, REQ-053)**: dikonfirmasi via DOM — card info 100% `<span>`, 0 elemen editable; satu-satunya elemen interactive di viewport atas adalah `<select>` "Pilih Alamat" milik form aktif (bukan bagian info card). PASSED.
- **Bongkar sebelum Muat (TC-N-014)**: pill kota bongkar collapsed berada dalam `<span>` TANPA ancestor `<button>` sama sekali sebelum Muat selesai — betul-betul non-interactive (bukan sekadar `disabled` visual). PASSED.
- **Required tanggal/foto (TC-N-034/035, REQ-056/057)**: helper error inline muncul tepat di bawah field ("Tanggal Selesai Muat harus diisi" / "Foto harus diisi"), Simpan gagal silent (tanpa toast), data tidak tersimpan. Keduanya PASSED — validasi backend/frontend bekerja benar.
- **Format tanggal invalid (TC-N-040)**: BLOCKED terkonfirmasi ulang — satu-satunya `<input>` di seluruh halaman Isi Data Tracking adalah `type=file`; field tanggal 100% calendar-picker (`<div>` trigger), tidak ada jalur ketik bebas.
- **FND-04 (foto ke-7, TC-N-036)** & **FND-05 (.gif diterima, TC-N-038)**: REPRODUCE PERSIS temuan run 20260829, 0 perubahan — dropzone `<input type=file>` di-unmount total dari DOM setelah 6 foto (bukan alert), dan `accept="image/*"` longgar meloloskan `.gif` tanpa penolakan. `.pdf` tetap ditolak benar dgn toast "File "…" bukan gambar.". **TC-N-037 (>4MB) PASSED** — toast "File "…" melebihi batas 4MB." muncul tepat, kontras dgn TC-N-036.

### Efek samping status order — catatan utk TC-P-024 (Batch 8, sudah passed tapi presisi perlu dicek ulang)
`ORD8500349511` (FTL) berubah status dari **"Belum Berangkat" → "Dalam Perjalanan"** HANYA setelah sub-tahap **"Berangkat ke Alamat"** disimpan (BUKAN setelah "Selesai Muat", yang di batch ini tidak pernah berhasil tersimpan krn selalu sengaja divalidasi-gagalkan utk TC-N-034/035/036/037/038). Ini mengindikasikan transisi status "Dalam Perjalanan" terjadi lebih awal dari yang diasumsikan TC-P-024 ("Status berubah menjadi Dalam Perjalanan setelah **Selesai Muat** tersimpan") — kemungkinan besar transisi sebenarnya terjadi di **"Berangkat ke Alamat"** (event tracking pertama apa pun), bukan spesifik "Selesai Muat". Rekomendasi: BA/executor Batch berikutnya re-verifikasi TC-P-024 dgn order baru yg diamati persis di titik "Berangkat ke Alamat" tersimpan (sebelum Selesai Muat) utk memastikan kapan tepatnya status berubah.

### Status akhir order Batch 9
| Order | Jenis | Aksi Batch 9 | Status akhir |
|---|---|---|---|
| `ORD7890024647` | FCL | Provisioning baru (Isi Data Manual + Jadwal Kapal Direct) | **Belum Berangkat** (cadangan Batch 13, bersih) |
| `ORD7889608568` | FCL | Provisioning baru + percobaan TC-P-057/058 (Nopol Bongkar diketik tapi Simpan gagal, tidak tersimpan) | **Belum Berangkat** (cadangan Batch 13, ada sisa input belum tersimpan) |
| `ORD7881082637` | FCL | Tidak sengaja diselesaikan penuh (Kapal Berlayar→Sandar→Bongkar) saat eksplorasi gating sebelum ditemukan entry point sopir-bongkar yg benar | **Selesai** (habis, jangan dipakai lagi) |
| `ORD8500349511` | FTL | TC-N-039/014/034/035/036/037/038 ("Berangkat ke Alamat" tersimpan, "Selesai Muat" sengaja tidak pernah lolos validasi) | **Dalam Perjalanan** (bukan lagi Belum Berangkat — lihat catatan efek samping di atas) |
| `ORD8500349489`, `ORD8500349263`, `ORD8500349232`, `ORD7798037664` | FTL | Tidak disentuh | **Belum Berangkat** (4 order, cadangan Batch 10-13) |
| `ORD7890180801` | FCL Multipoint | Tidak disentuh (dicadangkan) | **Belum Berangkat** (cadangan `TC-E-011`, Batch 10) |
| `ORD7890736370` (LTL), `ORD7890583351` (LCL) | LTL/LCL | Tidak disentuh (dicadangkan) | **Belum Berangkat** (cadangan Batch 11/12) |

## 2026-09-06 (lanjutan 8) — Batch 10 oms017-penugasan-tracking (runId 20260906-101116): 8 passed, 2 failed, 2 blocked — TC-E-011 sukses dimajukan penuh via gating, gotcha datepicker BARU (kolom jam/menit tertukar)

Detail hasil: `results/_batch10__oms017-penugasan-tracking__20260906-101116.json`. Semua via akun Admin (finance.roro1@gmail.com). GROUP C lanjutan (Batch 10/4).

### GOTCHA KRITIS BARU — popup jam & menit adalah 2 KOLOM dalam SATU grid, bukan 2 list terpisah berjauhan
Koreksi catatan Batch 8 ("2 daftar terpisah standar"): popup datepicker sebenarnya adalah **1 grid `grid-cols-2`** berisi 2 kolom bersebelahan — kolom KIRI (x≈570-615px) = jam 00-23 (class `...rounded-lg...h-8...`), kolom KANAN (x≈620-665px) = menit 00-59 (class identik). Kalender hari punya class BERBEDA (`h-9 w-9 mx-auto text-[13px]`). **Bug yang ditemukan saat eksekusi**: mencari tombol jam via `textContent.trim() === '10'` TANPA filter class akan match tombol KALENDER HARI "10" juga (krn tanggal & jam bisa share angka yg sama), dan filter x-position longgar (`x < 615`) tetap bisa salah tangkap sel kalender hari yg kebetulan x lebih kecil. **Fix wajib**: filter GABUNGAN `className.includes('rounded-lg') && rect.x < 615` utk jam, `rect.x >= 615` (atau `>615`) utk menit — JANGAN hanya x-position atau hanya text match. Direplikasi 6x sukses (Berangkat ke Alamat, Selesai Muat, Kapal Berlayar, Kapal Sandar, 2x Selesai Bongkar) pada `ORD7890024647` setelah fix diterapkan.

### TC-E-011 — order FCL Multidrop 3 kota berhasil dimajukan penuh via gating utk pertama kali di seluruh run
`ORD7890024647` (Kota Balikpapan → Kota Balikpapan/Kabupaten Sumenep/Kabupaten Banjar) dimajukan: Berangkat ke Alamat → Selesai Muat (No. Kontainer/Segel prefilled tetap editable, REQ-063 OK) → Kapal Berlayar → Kapal Sandar → BONGKAR kota 1 (Kota Balikpapan, Berangkat+Selesai Bongkar) → BONGKAR kota 2 (Kabupaten Sumenep, Berangkat+Selesai Bongkar). Kota 3 (Kabupaten Banjar) **sengaja disisakan 0/1** (belum disentuh) sesuai arahan batch. Badge status di `/penugasan-tracking` setelah 2/3 kota bongkar selesai: **`Dalam Perjalanan`** (bukan `Selesai`) — mengonfirmasi A-15/REQ-022 bahwa status baru menjadi Selesai setelah SEMUA kota drop selesai bongkar, bukan sebagian. Assertion "setelah kota terakhir → Selesai" sengaja TIDAK diuji (order dijaga di tahap ini utk Batch 13: `TC-N-053/054/055`, `TC-E-039/040`).

### TC-N-041 — No. Kontainer/Segel confirmed HILANG TOTAL (bukan disabled) pada form Selesai Bongkar
Pada `ORD7890024647` kota 1 (Kota Balikpapan), form "Selesai Bongkar" hanya berisi `Tanggal Selesai Bongkar *`, `Foto *`, `Keterangan` — 0 elemen No. Kontainer/No. Segel di DOM sama sekali (beda dari form Selesai Muat yg menampilkan keduanya editable). Sesuai REQ-063 (field hanya berlaku tahap Selesai Muat).

### TC-N-044/045/047 — precondition "Sopir bongkar sudah dipilih/terisi" TIDAK bisa dipenuhi krn Master Sopir vendor PT. Wahana Logistik kosong (gap Batch 9), tapi assertion inti tetap valid diuji
Master Sopir & Master Armada (mode "Pilih Dari Master") utk vendor ini SAMA-SAMA kosong ("Tidak ada sopir"/"Tidak ada armada" di combobox search). Armada Bongkar tetap bisa diisi via mode "Isi Data Manual" (No. Polisi bebas teks), tapi Sopir Bongkar TIDAK PUNYA mode manual sama sekali (REQ-065 gap, sama dgn Batch 9) — sehingga Sopir Bongkar mustahil diisi utk vendor ini saat ini. Ketiga skenario tetap **PASSED** krn validasi required per-field terbukti independen: klik Simpan dgn kombinasi field kosong berbeda-beda (semua kosong / Armada terisi+Sopir kosong / Armada terisi+Tanggal kosong) masing-masing memunculkan helper error yg TEPAT sesuai field yg kosong SAJA (field yg terisi tidak ikut error) — konfirmasi validasi form granular & benar, terlepas dari gap data sopir.

### TC-N-046 — REPRODUCE ulang FND-03/06 via entry point INLINE (bukan action-menu) khusus utk uji REQ-066 yang adil
Sesuai kompromi D-10 (entry point action-menu memang didesain valid balik ke daftar; hanya entry point inline dari dalam Isi Data Tracking yang REQ-066 wajibkan balik ke Isi Data Tracking), skenario ini dijalankan via tombol inline "Penugasan Sopir Bongkar" di dalam `/isi-tracking` `ORD7889608568`. Hasil: tetap redirect ke `/penugasan-tracking` (daftar) tanpa dialog konfirmasi — sama persis dgn Batch 9, mengonfirmasi bug ini genuinely tidak bergantung pada entry point.

### TC-N-060 — kosong.png (0 byte) & korup.jpg (byte acak, ekstensi .jpg valid) SAMA-SAMA lolos ke daftar lampiran tanpa alert
Diuji pada `ORD8500349511` (form Selesai Muat aktif). Tidak ada validasi content file sama sekali di client (hanya validasi ekstensi longgar + ukuran, konsisten FND-04/05). Tidak lanjut ke percobaan Simpan dgn tanggal valid (utk menghindari risiko keberhasilan simpan foto rusak secara permanen) — kandidat FND-14 (minor, disebutkan run 20260829) dikonfirmasi ulang persis dari sisi "masuk daftar tanpa alert".

### TC-N-042/043 — tetap blocked, tidak ada order CY-*/Door-CY baru ditemukan
Tidak dilakukan pencarian ulang (sesuai arahan, bukti definitif Batch 5 by intercept API sudah final).

### Gotcha tambahan — login field pakai getByPlaceholder, bukan getByLabel/type=email
Field email di `/login` adalah `<textbox>` custom dgn placeholder "Masukkan Email" (bukan `input[type=email]`/`name=email` standar) — `page.getByPlaceholder('Masukkan Email')` & `getByPlaceholder('Masukkan Password')` WAJIB dipakai; `getByLabel`/CSS attribute selector biasa gagal timeout. Login sempat tampak "gagal" (form reset ke placeholder kosong tanpa redirect) pada percobaan pertama setelah `clearCookies()` — ternyata hanya lambat (tombol sempat menampilkan teks loading "Masuk..." lalu reset visual sebelum akhirnya redirect sukses ke `/monitoring` di re-check berikutnya), BUKAN kegagalan kredensial. Juga direplikasi ulang: request GET context/events/detail bisa gagal `net::ERR_FAILED` tepat setelah POST event sukses (BUKAN kegagalan simpan, hanya refresh state) — solusi tetap `clearCookies()` + reload + login ulang, lalu reload halaman isi-tracking utk sinkronisasi.

### Status akhir order Batch 10
| Order | Jenis | Aksi Batch 10 | Status akhir |
|---|---|---|---|
| `ORD7890024647` | FCL Multidrop 3 kota | TC-N-041/TC-E-011: dimajukan penuh Muat→Kapal→2/3 kota Bongkar (Kota Balikpapan & Kabupaten Sumenep selesai; Kabupaten Banjar 0/1 disisakan sengaja) | **Dalam Perjalanan**, pasca-Selesai-Muat, 2/3 kota Bongkar selesai — cadangan Batch 13 (`TC-N-053/054/055`, `TC-E-039/040`), JANGAN selesaikan kota Banjar dulu |
| `ORD7889608568` | FCL | TC-N-044/045/047/046: berbagai percobaan Simpan form sopir bongkar (semua gagal tersimpan by design/negatif) + 1 Nopol manual "AUTOTEST-20260906-BONGKAR01" diketik tapi tidak tersimpan | **Belum Berangkat** (tidak berubah, cadangan Batch 13, sisa input tetap belum tersimpan seperti Batch 9) |
| `ORD8500349489` | FTL single-drop | TC-E-029: dibuka & dibaca saja, tidak ada Simpan | **Belum Berangkat** (bersih, tidak tersentuh) |
| `ORD8500349511` | FTL | TC-E-030 (reload check) + TC-N-060 (upload kosong/korup, tidak disimpan) | **Dalam Perjalanan** (tidak berubah dari Batch 9, form Selesai Muat masih pending/belum tersimpan) |
| `ORD8500349232`, `ORD7798037664` | FTL | Tidak disentuh | **Belum Berangkat** (2 order, cadangan Batch 13) |
| `ORD7890180801` | FCL Multipoint | Tidak disentuh | **Belum Berangkat** (cadangan, tidak jadi dipakai — TC-E-011 pakai ORD7890024647 sesuai arahan) |
| `ORD7890736370` (LTL), `ORD7890583351` (LCL) | LTL/LCL | Tidak disentuh | **Belum Berangkat** (cadangan Batch 11/12) |

## 2026-09-06 (lanjutan 9) — Batch 11 oms017-penugasan-tracking (runId 20260906-101116): 7 passed, 2 failed, 2 blocked — BATCH TERAKHIR GROUP C, gating baru sopir-bongkar ditemukan

Detail hasil: `results/_batch11__oms017-penugasan-tracking__20260906-101116.json`. Semua via akun Admin (finance.roro1@gmail.com).

### TEMUAN BARU KRITIS — gating "Tracking Berangkat Bongkar sudah diisi" mengunci Penugasan Sopir Bongkar level-ORDER (bukan per-kota)
Pada `ORD7890024647` (FCL Multidrop, Kota Balikpapan & Kabupaten Sumenep sudah "Berangkat Bongkar" tersimpan, Kabupaten Banjar masih 0/1 pending): tombol inline MAUPUN item action-menu "Penugasan Sopir Bongkar" **keduanya DISABLED** dengan `title`/tooltip persis **"Tidak bisa menugaskan sopir bongkar karena Tracking Berangkat Bongkar sudah diisi"** — berlaku untuk SELURUH order sekali ADA kota bongkar yang mulai tracking, bahkan untuk kota lain yang belum diproses sama sekali (Kabupaten Banjar). Ini BARU (tidak terdokumentasi Batch 9/10, karena order yang dipakai sebelumnya — `ORD7889608568` — selalu diuji dalam kondisi Belum Berangkat/bongkar sama sekali belum mulai). Kemungkinan besar ini **by-design** (mencegah re-assign sopir setelah proses bongkar berjalan), bukan bug — tapi berdampak langsung pada `TC-E-031` (lihat di bawah) dan berpotensi relevan untuk Batch 13 bila ada rencana uji sopir-bongkar pada order yang sudah maju.

### TC-E-031 — BLOCKED, precondition mustahil dipenuhi dgn data pool saat ini
Order yang dialokasikan (`ORD7890024647`) kena gating di atas. Dicari order substitusi FCL/LCL dgn "Selesai Muat tersimpan + Bongkar belum mulai" via route-intercept API (`limit=100`, 79 baris total, lihat teknik recon lanjutan 3): hanya 4 kandidat ditemukan (`ORD8338721286`, `ORD8148775658`, `ORD7466529895`, `ORD7466106555`), **SEMUA milik vendor read-only** (PT. Jaya Karsa / PT. Intan Emas Permata, banner "Anda hanya dapat melihat data ini (mode read-only)") dan **SEMUA sudah punya data sopir bongkar tersimpan dari run 20260829** — tidak layak untuk uji tulis baru. Satu-satunya order FCL/LCL writable (vendor PT. Wahana Logistik) yang tersisa di pool adalah `ORD7889608568` (dicadangkan TC-E-032/033/035, Belum Berangkat total — precondition "Selesai Muat tersimpan" jg tak terpenuhi bila dipakai) dan `ORD7890024647` sendiri (kena gating). **Rekomendasi BA/executor Batch berikutnya**: sediakan 1 order FCL/LCL baru yang baru saja selesai Muat (Berangkat Bongkar BELUM diisi sama sekali di kota manapun) khusus untuk re-test TC-E-031 presisi.

### TC-E-032/033 — REPRODUCE ulang FND-08 (tidak ada dialog Batal sama sekali)
Diuji di `ORD7889608568` (Belum Berangkat, sopir-bongkar masih enabled). Entry inline (TC-E-032, No. Polisi diisi manual "B 7930 HD" lalu Batal) dan entry action-menu (TC-E-033, form pristine lalu Batal) **keduanya** langsung navigasi tanpa dialog konfirmasi apa pun (`page.on('dialog')` tidak pernah fire di kedua kasus). TC-E-032 tetap FAILED (FND-08, sesuai arahan) karena assertion "tetap di form setelah klik Tidak" mustahil dipenuhi; TC-E-033 tetap PASSED karena expected-nya memang balik ke daftar (D-10). `ORD7889608568` dikonfirmasi tetap Belum Berangkat, 0/1 alamat selesai di akhir kedua test (tidak ada Simpan sama sekali).

### TC-E-035 — Door-Door dikonfirmasi via halaman Edit Penugasan (bukan Tambah Penugasan baru, krn 0 order CY-* tersedia)
Field "Armada Muat *"/"Sopir Muat *" dicek via `/penugasan-tracking/{uuid}/edit` (bukan wizard Tambah Penugasan) pada 2 order Door-to-Door: FCL `ORD7889608568` dan LCL `ORD7890583351` — keduanya menampilkan field tsb sesuai ekspektasi baris Door-Door, dan action-menu keduanya memuat "Penugasan Sopir Bongkar". Tidak ada Simpan/perubahan pada kedua order (hanya inspeksi field). Door-CY/CY-Door/CY-CY tetap blocked (0 order, konsisten Batch 5). Status keseluruhan skenario: **blocked** (kebijakan satu-skenario-satu-verdict sesuai arahan batch), bukan passed seperti run 20260829 (perubahan kebijakan verdict, bukan perubahan temuan aplikasi).

### TC-E-024/025/026/027/028/042 — digabung efisien dalam 1 submission "Selesai Muat" (`ORD8500349511`), semua REPRODUCE/konsisten temuan lama
Datepicker Isi Data Tracking dikonfirmasi **BUKAN** grid gabungan `grid-cols-2` seperti dugaan Batch 10 (itu spesifik screen Tambah Penugasan) — di halaman Isi Data Tracking, jam (24 tombol) dan menit (60 tombol) berada dalam **2 container SIBLING TERPISAH** (bukan 1 grid tercampur); cara paling robust & general: cari container via **jumlah direct-child BUTTON** (`42` = grid hari, `24` = jam, `60` = menit) — tidak perlu bergantung pada class Tailwind spesifik yg bisa berubah. Helper ini berhasil dipakai utk seluruh 6 field tanggal batch ini (Berangkat ke Alamat x2, Selesai Muat, Selesai Bongkar percobaan x2) tanpa error.

**FND-07 (boundary 4MB) dikonfirmasi ulang dgn presisi baru**: client-side menerima file s.d. **persis 4.194.304 byte** (2^22, biner MiB inklusif) dan menolak mulai **4.194.305 byte** (pesan literal `File "<nama>" melebihi batas 4MB.`, thumbnail tidak ditambahkan) — BUKAN di 4.000.000 byte (desimal) seperti literal testData beberapa skenario. File `TC-E-026` (4.000.100 byte) karena itu **selalu diterima & tersimpan** (gap-desain, bukan bug baru). Method pengujian: upload varian besar SELALU dilakukan sendirian dulu (belum di-Simpan) utk isolasi, baru dihapus via tombol X merah pada thumbnail sebelum lanjut ke kombinasi final — terbukti reliable, tidak perlu reload penuh utk kasus "upload lalu batal sebelum submit" (beda dgn kasus "submit gagal/sukses" yang tetap wajib reload).

**Efek samping status order (dicatat, bukan bug)**: `ORD8500349489` (dipakai TC-E-026) berubah Belum Berangkat → Dalam Perjalanan krn precondition "buka form Selesai Muat" mengharuskan "Berangkat ke Alamat" tersimpan dulu — tak terhindarkan, order ini tidak termasuk cadangan Batch 12/13 sehingga aman.

### Status akhir order Batch 11 (SEMUA cadangan Batch 12/13 dikonfirmasi UTUH via route-intercept API di akhir batch)
| Order | Jenis | Aksi Batch 11 | Status akhir |
|---|---|---|---|
| `ORD8500349511` | FTL | TC-E-024/025/027/028/042: Selesai Muat tersimpan (6 foto + keterangan 2000 char + tanggal 29/02/2028) | **Dalam Perjalanan**, Bongkar (Kabupaten Sumenep) masih pending — boleh dilanjut Batch berikutnya bila perlu |
| `ORD8500349489` | FTL | TC-E-034 (read-only check) lalu TC-E-026: Berangkat+Selesai Muat tersimpan (Kota Balikpapan 1/1), percobaan Selesai Bongkar (Kabupaten Sumenep) file besar ditolak, tidak tersimpan | **Dalam Perjalanan** (berubah dari Belum Berangkat — efek samping tak terhindarkan, order tidak dicadangkan) |
| `ORD7890024647` | FCL Multidrop | TC-E-031: hanya klik elemen disabled (tidak ada aksi tereksekusi) | **TIDAK BERUBAH** — Kota Balikpapan & Kabupaten Sumenep tetap 1/1, Kabupaten Banjar tetap 0/1 (aman utk Batch 13) |
| `ORD7889608568` | FCL | TC-E-032/033/035: isi No. Polisi manual lalu Batal (2x, tidak pernah Simpan) + inspeksi Edit Penugasan | **TIDAK BERUBAH** — tetap Belum Berangkat, 0/1 alamat selesai (aman utk Batch 13) |
| `ORD7890583351` (LCL) | LCL | TC-E-035: inspeksi Edit Penugasan + action-menu saja | **TIDAK BERUBAH** — tetap Belum Berangkat |
| `ORD8500349517`, `ORD8500349232`, `ORD7798037664` (FTL), `ORD7890180801` (FCL Multipoint), `ORD7890736370` (LTL) | — | Tidak disentuh sama sekali | **TIDAK BERUBAH** — dikonfirmasi via route-intercept API di akhir batch |

GROUP C (Batch 8–11) selesai. Ringkasan kumulatif GROUP C: entry point & struktur Isi Data Tracking terpetakan penuh, FND-03/06/07/08/14 & gap REQ-064/065 (sopir bongkar no-manual) dikonfirmasi berulang kali independen, 1 gating baru ditemukan (lock order-level pasca-Berangkat-Bongkar). Siap lanjut GROUP D+E (Batch 12–13, Detail/Riwayat & Edit Penugasan).

## 2026-09-06 (lanjutan 10) — Batch 12 oms017-penugasan-tracking (runId 20260906-101116): 10 passed, 5 failed — BUG BARU KRITIS Edit Penugasan Simpan selalu 404, GROUP D dipetakan penuh

Detail hasil: `results/_batch12__oms017-penugasan-tracking__20260906-101116.json`. Semua via akun Admin (finance.roro1@gmail.com). GROUP D (Detail/Riwayat) + awal GROUP E (Edit).

### BUG BARU KRITIS — FND-OMS017-16: Simpan pada Edit Penugasan SELALU gagal (404 "Penugasan tidak ditemukan"), blocking GROUP E secara menyeluruh
Provisioning awal batch berhasil (2 order FTL vendor jayaswastikaqw via Tambah Penugasan — **H1** `ORD8500349505` id tracking `3903af90-9fe7-4429-9924-8230537375f9`/penugasanId `e19c1882-a63b-4993-b6af-a13112d87ee2`, **H2** `ORD8500349488` id tracking `fb62e777-4274-4cb9-8ed5-e62726aacfd8`/penugasanId `92b69467-1658-4cce-b98a-058ad94a97d1`, keduanya via `POST /api/penugasan` 201). Namun rencana "2x Edit Penugasan berurutan pada H1" (untuk precondition TC-P-062/TC-E-038) **GAGAL TOTAL**: setiap klik Simpan pada halaman `/penugasan-tracking/{id}/edit` memicu `PUT https://apioms-staging.prahu-hub.com/api/penugasan/{id}` yang **SELALU** mengembalikan **404 `{"success":false,"message":"Penugasan tidak ditemukan"}`** — dikonfirmasi reproducible pada 2 order independen (H1 yang baru dibuat, DAN `ORD8500349263` — order lama dari Batch 7, 2+ jam sebelumnya) via 3 percobaan terpisah (1x transient `net::ERR_CONNECTION_CLOSED` diikuti retry bersih 404, lalu 1x lagi 404 dengan `page.waitForResponse` eksplisit).

**Root cause kemungkinan besar**: setiap baris di `GET /api/penugasan-tracking` punya DUA id berbeda — `id` (row PenugasanTracking, dipakai di URL `/penugasan-tracking/{id}/edit` DAN oleh tombol Edit action-menu, dikonfirmasi via klik UI langsung bukan cuma manual) dan `penugasanId` (field terpisah, entitas Penugasan sebenarnya). **Dikonfirmasi via query API: seluruh 81 baris di database saat ini punya `id !== penugasanId`** (0 dari 81 sama) — pola ini sudah ada sejak record 2026-08-22, bukan hal baru. Namun PUT edit sekarang mengirim `{id}` (tracking id) sebagai path param ke `/api/penugasan/{id}`, padahal backend tampaknya mengharapkan `penugasanId`. Karena run 20260829 sebelumnya berhasil Simpan edit pada beberapa order (tercatat di summary triase: TC-N-053/054/055 FCL berhasil ubah field, ORD7888480114 punya riwayat hasil edit sukses), ini **kemungkinan REGRESI BARU** pada deploy antara 2026-08-29 dan 2026-09-06, bukan bug lama yang baru ketemu.

**Percobaan diagnosis lanjutan** (rewrite path PUT dari tracking-id ke penugasanId via `page.route()`) **diblokir classifier auto-mode** ("Blocked by classifier") karena dianggap modifikasi request mencurigakan — dihentikan sesuai kebijakan, tidak dipaksakan.

**Dampak ke Batch 12 & 13**:
- TC-P-062/TC-E-038 (butuh riwayat parsial FRESH pada H1) **tidak bisa dieksekusi sesuai rencana** — dipivot memakai data historis EXISTING dari run 20260829 yang masih preserved di database (`ORD7888480114`, 3 entri riwayat asli, tidak disentuh/reserved batch ini) untuk tetap menguji assertion inti (dan berhasil mereproduksi FND-OMS017-10 dua arah).
- H1 (`ORD8500349505`) berakhir dengan **0 entri riwayat perubahan** (edit gagal, data tidak berubah — unit tetap `AUTOTEST-20260906-H1XY01`, sopir `AUTOTEST-20260906-SopirH1`).
- **Batch 13 (GROUP E, 14 skenario editability-per-status) KEMUNGKINAN BESAR SELURUHNYA BLOCKED** oleh bug ini — TC-N-056, TC-P-066/067/068/069, TC-N-052/053/054/055, TC-N-058, TC-E-039/040/041 semuanya bergantung pada Simpan berhasil untuk membuktikan field locked/unlocked per status. **Rekomendasi eksplisit untuk executor Batch 13**: verifikasi ULANG dulu apakah bug ini masih reproduce (kemungkinan hotfix di antara batch) sebelum eksekusi; bila masih reproduce, tandai skenario yang butuh Simpan berhasil sebagai `blocked` (bukan `failed`) dengan rujukan FND-OMS017-16, JANGAN retry berulang kali (sudah dikonfirmasi 3x independen di batch ini).

### Struktur nyata halaman Detail Penugasan (REQ-068..074, D-02/06/07/12/15) — dipetakan lengkap
- Route: `/penugasan-tracking/{id}` langsung (BUKAN `/detail/{id}`), UUID row tracking sama dengan yang dipakai `/edit`.
- 3 section top-level (accordion, dapat expand/collapse via klik header): **Detail Data Order**, **Informasi Penugasan**, **History Tracking**. `inputCount=0` di seluruh halaman (100% read-only, `<span>`/teks statis) — konfirmasi TC-N-048/TC-N-053(lama).
- Detail Data Order FTL/LTL: ID Order, Jenis Order, Kota Asal, Kota Tujuan, Tanggal Permintaan Muat, **Jenis Armada** (label ini, bukan Jenis Kontainer), Kapasitas Armada — Metode Pengiriman TIDAK ADA (REQ-069 FTL/LTL confirmed).
- Detail Data Order FCL: tambahan Pelabuhan Asal/Tujuan, **Jenis Kontainer**+Kapasitas Kontainer, **Metode Pengiriman** — semua ada.
- Informasi Penugasan FTL/LTL: No. Polisi, Jenis Armada (sering '-' bila mode manual tanpa jenis spesifik), trigger **'Lihat Detail'** (Riwayat Penugasan, text-link di dalam section), Nama Sopir, No. WA Sopir.
- Informasi Penugasan FCL: dipecah 'Informasi Umum' (No. Kontainer/No. Segel/trigger Riwayat), 'Penugasan Muat' (No. Polisi/Jenis Armada/Metode Penugasan/Nama Sopir/No. WA), 'Penugasan Bongkar' (field sama, sering '-' semua bila proses bongkar dilakukan lewat Isi Data Tracking langsung tanpa fitur Penugasan Sopir Bongkar terpisah), 'Jadwal Kapal' (Pelayaran/Nama Kapal/Voyage/Closing Time/ETD/ETA).
- History Tracking: HANYA 2 tab di SEMUA jenis shipment — **'Per Lokasi'** + **'Timeline'**. Tab 'Per Tahapan' yang didesain khusus FCL **TIDAK ADA di implementasi manapun** — re-konfirmasi FND-OMS017-09 (gap-desain lama, D-06) dgn order berbeda dari run sebelumnya (LTL `ORD7890736370` vs FCL `ORD7881082637` Selesai).
- **Tidak ada badge/representasi status APA PUN** di halaman Detail (area antara judul "Detail Penugasan" dan section pertama hanya berisi tombol "Kembali") — re-konfirmasi FND-OMS017-13 (gap-desain lama) pada 2 order (IN_PROGRESS & SELESAI), kontras dengan daftar yang badge-nya jelas.

### Pop-up "Riwayat Penugasan" vs halaman penuh "/riwayat" — 2 mekanisme BERBEDA, satu benar satu buggy
- **Pop-up** (trigger 'Lihat Detail' di section Informasi Penugasan, Detail page): modal card per-entri (Tanggal Perubahan/Armada/Sopir), TAPI **snapshot penuh TANPA logic diff/strip** — field yang TIDAK berubah tetap menampilkan nilai asli apa adanya, bukan '-' (REQ-074 dilanggar). Re-konfirmasi **FND-OMS017-10** dua arah (nopol-only → Sopir tidak di-strip; sopir-only → Armada tidak di-strip) pakai data historis `ORD7888480114`.
- **Setiap penugasan baru otomatis dapat 1 entri "Penugasan Awal"** di pop-up ini SAAT DIBUAT (bukan hanya saat diedit) — **TEMUAN BARU FND-OMS017-17** (gap-desain, terkait D-07): akibatnya true empty-state (0 entri) tidak pernah reachable untuk order manapun yang sudah py penugasan, precondition "penugasan belum pernah diubah" tidak menghasilkan modal kosong seperti diasumsikan skenario TC-N-051 — perlu klarifikasi PO apakah "Penugasan Awal" seharusnya dihitung sbg riwayat atau tidak.
- Modal ditutup via `button[aria-label="Tutup"]` (ikon X) — **Escape TIDAK berfungsi** untuk modal ini (dicoba, gagal). Setelah tutup, tetap di halaman Detail (tidak navigasi keluar).
- **Halaman penuh** `/penugasan-tracking/{id}/riwayat` (trigger: action-menu 'Riwayat Perubahan' di daftar) — tabel ringkas No/Tanggal Perubahan/Diubah Oleh/Total Perubahan (HANYA menghitung edit sungguhan, TIDAK termasuk 'Penugasan Awal' — konsisten dgn count di pop-up dikurangi 1). Klik baris '1 Perubahan' expand jadi 'Data Penugasan: No | Field | Data Sebelum | Data Sesudah' — **HANYA menampilkan field yang benar-benar berubah** (diff-list sungguhan, field tak berubah tidak muncul barisnya sama sekali). Halaman ini justru **BENAR** mengimplementasikan semangat REQ-074, kontras dgn pop-up yang buggy.

### Halaman Edit Penugasan — Info Order block (REQ-075, D-16) terverifikasi lengkap
Heading tetap **"Edit Penugasan"** (bukan "Tambah Penugasan" seperti mockup 149/150, sesuai D-16). Blok "Info Order" FCL: ID Order, Vendor, Kota Asal/Tujuan, Pelabuhan Asal/Tujuan, Jenis Kontainer, Jumlah Kontainer, Metode Pengiriman, Tanggal Permintaan Muat — LENGKAP. Blok "Info Order" FTL/LTL: Jenis Armada + Tanggal Permintaan Muat saja, TANPA Pelabuhan/Metode Pengiriman — sesuai REQ-075. Tombol **"Batal"** menavigasi LANGSUNG ke `/penugasan-tracking` (daftar) **TANPA dialog konfirmasi apa pun** (native maupun custom) — berbeda dari asumsi arahan batch yang menduga ada native `confirm()`; aman dipakai untuk keluar dari Edit tanpa menyimpan.

### Order yang dipakai/status akhir Batch 12 (SEMUA cadangan Batch 13 dikonfirmasi UTUH via route-intercept API di akhir batch)
| Order | Jenis | Aksi Batch 12 | Status akhir |
|---|---|---|---|
| **H1** `ORD8500349505` | FTL | Provisioning baru (Tambah Penugasan sukses) + 3x percobaan Edit-Simpan GAGAL (404, bug FND-OMS017-16, tidak ada data berubah) | **Belum Berangkat**, **0 entri riwayat** — cadangan Batch 13 (fresh, tapi Edit-Save masih rusak) |
| **H2** `ORD8500349488` | FTL | Provisioning baru (Tambah Penugasan sukses) + TC-N-051 (pop-up, 1 entri 'Penugasan Awal') + TC-N-049 (Detail) + TC-P-065 (Edit, Batal tanpa simpan) | **Belum Berangkat**, 1 entri 'Penugasan Awal' — cadangan Batch 13 |
| `ORD7890736370` (LTL) | LTL | TC-P-059 + TC-E-036 (Detail read-only, TIDAK dimajukan) | **TIDAK BERUBAH** — tetap Belum Berangkat, 0/2 tahapan di kedua kota |
| `ORD7881082637` (FCL) | FCL | TC-P-060 + TC-E-036 (Detail read-only, Selesai) | **TIDAK BERUBAH** — tetap Selesai |
| `ORD8500349521` (FTL) | FTL | TC-E-012 (Detail read-only, Selesai) | **TIDAK BERUBAH** — tetap Selesai |
| `ORD7890024647` (FCL Multidrop) | FCL | TC-E-012 (Detail read-only) | **TIDAK BERUBAH** — Kab. Banjar tetap 0/1 bongkar (aman Batch 13) |
| `ORD7889608568` (FCL) | FCL | TC-P-064 (Edit dibuka, dibaca, Batal TANPA simpan) | **TIDAK BERUBAH** — tetap Belum Berangkat |
| `ORD7890180801` (FCL Multipoint) | FCL | Tidak disentuh | **TIDAK BERUBAH** |
| `ORD8500349232` (FTL) | FTL | Tidak disentuh | **TIDAK BERUBAH** |
| `ORD7890583351` (LCL) | LCL | Tidak disentuh | **TIDAK BERUBAH** |
| `ORD8500349517` (FTL) | FTL | Tidak disentuh | **TIDAK BERUBAH** |
| `ORD7888480114` (FTL, sisa run 20260829) | FTL | TC-P-061/062/063, TC-E-037/038 (Detail + pop-up + /riwayat, SEMUA read-only) | **TIDAK BERUBAH** — tetap Belum Berangkat, 3 entri riwayat historis utuh |
| `ORD8500349263` (FTL, sisa Batch 7) | FTL | Dipakai HANYA untuk diagnosis bug Edit-Simpan (1x percobaan fill+Simpan gagal 404) | **TIDAK BERUBAH** — PUT gagal, No. Polisi tetap nilai lama |

GROUP D selesai dipetakan penuh (Detail + pop-up Riwayat + halaman /riwayat). GROUP E baru dimulai sebagian (Info Order block Edit saja, read-only) — **Batch 13 WAJIB baca bug FND-OMS017-16 di atas sebelum eksekusi editability tests**.

## 2026-09-06 (lanjutan 11) — Batch 13 oms017-penugasan-tracking (runId 20260906-101116): 9 passed, 5 failed — BATCH TERAKHIR (GROUP E selesai), FND-OMS017-16 REPRODUCE lintas FTL+FCL, mekanisme "Tidak Bisa Edit" full-page block ditemukan, 1 gap baru FND-OMS017-18

Detail hasil: `results/_batch13__oms017-penugasan-tracking__20260906-101116.json`. Semua via akun Admin (finance.roro1@gmail.com). GROUP E (Edit Penugasan, editability per status) — modul oms017-penugasan-tracking selesai dieksekusi penuh (kecuali `TC-S-*` yang di-skip sesuai plan).

### FND-OMS017-16 (Simpan Edit selalu 404) — REPRODUCE 5x independen, DIKONFIRMASI LINTAS FTL & FCL
Diverifikasi ULANG di awal batch (sesuai rekomendasi Batch 12) sebelum full eksekusi — **bug MASIH REPRODUCE, tidak ada hotfix**. Total 5 percobaan Simpan lengkap (dgn `page.on('response')`/`waitForResponse` eksplisit menangkap `PUT /api/penugasan/{id}`), SEMUA mengembalikan **404 `{"success":false,"message":"Penugasan tidak ditemukan"}`**, pada 2 order BARU yang belum pernah disentuh Edit sebelumnya (tidak hanya order yang sudah "tercemar" Batch 12):
- `TC-P-066` (FTL `ORD8500349232`, ubah nopol+sopir)
- `TC-P-067` (FCL `ORD7889608568`, ubah nopol/armada)
- `TC-P-068` (FCL, sama, ubah No. Kontainer/Segel)
- `TC-P-069` (FCL, sama, ubah jadwal kapal)

Kesimpulan diperkuat: bug bersifat **sistemik & permanen** (bukan flaky/spesifik 1-2 order), berlaku utk **FTL maupun FCL** (root cause sama: frontend kirim `id` row tracking, backend butuh `penugasanId`, lihat detail Batch 12). Skenario yang butuh Simpan berhasil (`TC-P-066/067/068/069`) SEMUA `failed` dgn `bugCandidate=FND-OMS017-16` + screenshot. **FND-OMS017-12** (ETD/ETA mundur 1 hari saat tersimpan, run 20260829) **TIDAK DAPAT diverifikasi ulang** di `TC-P-069` krn Simpan tidak pernah berhasil — tidak ada nilai tersimpan utk dibandingkan; status FND-12 tetap "unconfirmed di run ini" (bukan resolved, bukan reproduce, murni tak teruji krn diblok bug lain).

**Efek samping ditemukan**: order `ORD7889608568` punya data Jadwal Kapal stale (ETD sama persis dgn tanggal Closing Time, kemungkinan residu FND-12 dari provisioning Batch 9) yang memicu validasi KLIEN "Berangkat (ETD) harus setelah Closing Time" dan memblokir Simpan **apa pun** field yang diubah (bukan cuma jadwal kapal) — harus diperbaiki manual (ETD digeser ke 12/09/2026) sebelum tiap percobaan `TC-P-067/068/069` bisa mencapai tahap PUT sungguhan. Perbaikan ini tidak pernah persisten (reset tiap reload) krn Simpan selalu gagal 404.

### TEMUAN MEKANISME BARU — "Tidak Bisa Edit" full-page block menggantikan field-level lock, REGRESI POSITIF utk FND-OMS017-11 (FCL kini terkunci)
Berbeda dari asumsi desain (field individual disabled DI DALAM halaman Edit yang tetap bisa diakses) maupun dari temuan run 20260829 (FND-OMS017-11: field FCL dulu **tidak** terkunci sama sekali), implementasi SEKARANG memblokir **SELURUH halaman Edit** begitu status order lewat "Belum Berangkat"/"Menunggu Proses", berlaku identik utk FTL **dan** FCL:
- **Status "Dalam Perjalanan"/"Selesai Muat"**: item menu "Edit" pada action-menu daftar **disabled** (`disabled=""`) dgn `title="Tidak bisa edit karena sudah proses pengiriman"`. Navigasi **langsung via URL** ke `/penugasan-tracking/{uuid}/edit` JUGA diblokir di level aplikasi (bukan sekadar UI menu) — halaman merender heading **"Tidak Bisa Edit"** + pesan **"Tidak bisa melakukan edit karena sudah dalam proses pengiriman."** + tombol "Kembali ke List", dgn **0 elemen form** di DOM sama sekali. Dikonfirmasi identik pada FTL (`ORD8500349517`, TC-N-052) dan FCL (`ORD7890024647`, TC-N-053/054/055/E-039).
- **Status "Selesai"**: lebih tegas lagi — item menu "Edit" **dihapus total** dari action-menu (bukan sekadar disabled), tersisa hanya "Detail" + "Riwayat Perubahan". Dikonfirmasi pada FTL `ORD8500349521` & FCL `ORD7881082637` (TC-N-058).
- **Boundary transisi**: dikonfirmasi presisi via `TC-E-040` (`ORD7890180801`, FCL Multipoint) — field Edit enabled penuh sebelum tracking dimulai; SEGERA setelah 1x "Berangkat ke Alamat" + 1x "Selesai Muat" tersimpan (progres 1/2 alamat), navigasi ulang ke `/edit` **langsung** menampilkan "Tidak Bisa Edit" — tidak ada window waktu dgn kondisi field-level partial-lock.

**Dampak ke assertion**: `TC-N-052/053/054/055/058` dan bagian pertama `TC-E-039` seluruhnya **PASSED** berdasarkan **intent** REQ-076/077/078/079 (field benar-benar tidak bisa diubah), meski mekanisme literal berbeda dari yang diasumsikan skenario (field-level disabled vs full-page block). Ini kemungkinan **fix backend/frontend** yang sekaligus jadi akar bug FND-OMS017-16 (kemungkinan perubahan pada API `/api/penugasan/{id}` & guard status terjadi dalam deployment yang sama antara run 20260829 dan 20260906) — rekomendasi triage: BA/dev sebaiknya mengubah pendekatan jadi field-level lock granular sesuai spec asli (No. Kontainer/Segel terkunci lebih awal drpd armada/jadwal kapal, sesuai REQ-077 vs REQ-078) drpd full-page block yang saat ini over-restrictive, SEKALIGUS memperbaiki bug 404 pada endpoint PUT.

### TC-E-039 bagian 2 (No. Kontainer editable di form Selesai Muat) — dikonfirmasi LANGSUNG, bukan referensi
Field No. Kontainer pada form tracking "Selesai Muat" (`ORD7890180801`, alamat "IK - BPN Platinum") diuji eksplisit: `isDisabled()=false`, `fill()` berhasil mengubah value — kontras total dgn halaman Edit yang di-block penuh untuk order lain yang sudah lewat tahap ini. REQ-063 (editable di form tracking) vs REQ-078/A-05 (terkunci di halaman Edit) terbukti berdampingan benar, dua konteks halaman berbeda.

### FND-OMS017-18 (BARU, gap-desain) — Field No. Polisi pada Edit tidak bisa dikosongkan, tapi TANPA helper error/border error
`TC-N-056` (`ORD8500349488`/H2): field "No. Polisi" **menolak jadi string kosong secara silent** — `fill('')`, `Backspace`, `Ctrl+A+Delete` semua di-revert otomatis ke nilai asli tanpa border merah/pesan error apa pun (field TETAP menerima string non-kosong apa pun termasuk 1 spasi). Karena state benar-benar kosong tak pernah tercapai, klik Simpan membuka dialog "Konfirmasi Penugasan" seperti biasa dgn nilai asli (bukan kosong) — assertion literal REQ-034 ("helper error + border error") **tidak pernah terpenuhi** meski efek praktis (data tak pernah tersimpan kosong) tercapai. Diverifikasi silang di 2 order (H2 & `ORD8500349232`) — pola umum, bukan spesifik 1 order.

### TC-E-041 — precondition diadaptasi (1 entri riwayat, bukan 2), assertion inti tetap valid
Precondition asli skenario ("2 entri riwayat") tidak bisa dipenuhi krn FND-OMS017-16 mencegah pembuatan riwayat baru sejak Batch 12. Diuji dgn 1 entri "Penugasan Awal" (H2) — prinsip sama: hitung entri sebelum & sesudah Batal, harus tetap sama. Hasil: PASSED, tombol "Batal" (tanpa dialog konfirmasi, konsisten Batch 12) tidak menambah riwayat maupun mengubah data server.

### Tugas tambahan — 3 screenshot tertinggal Batch 4 & 11 dilengkapi
- `TC-P-030` (`results/_batch04__...json`): screenshot ringkasan Tambah Penugasan order `ORD8500349457` (masih unassigned) menampilkan "Kota Tujuan: Kabupaten Sumenep" (bukti FND-15, prefix "Kabupaten" bukan "Kab."). Keluar via Batal (dialog native `confirm()` "Perubahan akan hilang..." auto-dismiss) lalu navigasi langsung ke `/penugasan-tracking` — order TETAP unassigned, tidak ada data ditulis.
- `TC-E-026` (`results/_batch11__...json`): screenshot upload file 4.000.100 byte (`bukti-4mb-plus100.png`) pada form "Selesai Bongkar" `ORD8500349489` (Dalam Perjalanan, Kabupaten Sumenep) — diterima tanpa alert (thumbnail muncul). TIDAK di-Simpan, halaman ditinggalkan.
- `TC-E-032` (`results/_batch11__...json`): screenshot halaman `/penugasan-tracking` (daftar) setelah klik Batal pada form Penugasan Sopir Bongkar `ORD7889608568` (No. Polisi Bongkar diisi `AUTOTEST-20260906-B7930HD` lalu Batal) — dieksekusi SETELAH `TC-P-067/068/069` selesai sesuai arahan, order dikonfirmasi tetap "Belum Berangkat" (tidak berubah).
Ketiga file JSON diupdate HANYA field `screenshot` via skrip Python kecil, field lain tidak disentuh.

### Status akhir seluruh order run ini (untuk pembersihan data `AUTOTEST-20260906-*`)
| Order | Jenis | Status akhir | Catatan |
|---|---|---|---|
| `ORD8500349232` | FTL | **Belum Berangkat** | Nopol/sopir server tetap `AUTOTEST-20260906-B0019XY`/`SopirE019` (TC-P-066 gagal simpan) |
| `ORD8500349488` (H2) | FTL | **Belum Berangkat**, 1 entri riwayat "Penugasan Awal" | Nopol/sopir tetap `AUTOTEST-20260906-H2XY01`/`SopirH2` |
| `ORD8500349505` (H1) | FTL | **Belum Berangkat**, 0 entri riwayat | Tidak disentuh batch ini (cadangan tak terpakai) |
| `ORD8500349517` | FTL | **Dalam Perjalanan** | Tidak disentuh (hanya cek Edit-blocked) |
| `ORD7889608568` | FCL | **Belum Berangkat** | TC-P-067/068/069 gagal simpan (404) + TC-E-032 (Batal); Nopol/Kontainer/Segel/Kapal server TIDAK berubah |
| `ORD7890024647` | FCL Multidrop | **Dalam Perjalanan**, 2/3 kota Bongkar selesai (Kab. Banjar tetap 0/1) | Hanya cek Edit-blocked, tidak disentuh lebih lanjut |
| `ORD7890180801` | FCL Multipoint | **Dalam Perjalanan** (BARU, dulu Belum Berangkat) | TC-E-040: Muat kota 1 (Berangkat+Selesai) tersimpan sungguhan, No. Kontainer diubah jd `AUTOTEST-20260906-CTN001-EDIT`, 1 foto `kecil2.png` diupload — kota ke-2 (IK - BPN Market) masih 0/2, sengaja disisakan |
| `ORD8500349521` | FTL | **Selesai** | Tidak disentuh, hanya cek menu Edit tidak ada |
| `ORD7881082637` | FCL | **Selesai** | Tidak disentuh, hanya cek menu Edit tidak ada |
| `ORD8500349489` | FTL | **Dalam Perjalanan** (tidak berubah) | TC-E-026: upload 4.000.100 byte di form Selesai Bongkar, TIDAK disimpan |
| `ORD8500349457` | FTL | **Belum Berangkat, TETAP UNASSIGNED** | TC-P-030: dibuka di wizard Tambah Penugasan lalu Batal, 0 data ditulis |
| `ORD7890736370` (LTL), `ORD7890583351` (LCL), `ORD7888480114`, `ORD8500349263` | — | Tidak disentuh | Sesuai arahan ("tidak perlu disentuh") |

**MODUL oms017-penugasan-tracking SELESAI DIEKSEKUSI PENUH** (Batch 1-13, kecuali `TC-S-*` yang di-skip sesuai keputusan plan). Ringkasan bug/gap kumulatif signifikan: FND-OMS017-16 (Simpan Edit 404, KRITIS, blocking) & FND-OMS017-18 (No. Polisi tak bisa dikosongkan tanpa feedback, BARU) direkomendasikan sbg prioritas perbaikan tertinggi sebelum re-run berikutnya; FND-OMS017-11 kemungkinan sudah diperbaiki (mekanisme baru "Tidak Bisa Edit") tapi perlu dikonfirmasi PO apakah full-page-block ini disengaja atau efek samping fix lain.


## 2026-09-07 — oms000-jenis-produk run 20260907-115730

Cakupan 174 non-stress dan perubahan entitlement tenant testing telah disetujui pengguna. Konfigurasi API ada di config/env.md (jangan salin kunci). Hasil {'passed': 11, 'blocked': 153, 'failed': 10, 'skipped': 22}. Guard tiga gagal identik NEG-009/010/011 (400 expected vs 422 actual) menghentikan eksekusi; skenario berikutnya blocked. Pemulihan OMS + 6 add-on berhasil dan diverifikasi lewat /api/system/status setelah propagasi. GET entitlement/client usulan 404, namun PATCH dan read-back status aplikasi berfungsi. JSON rusak menghasilkan 500 (FND-OMS000-RUN-01, probable major). Perbedaan status, duplikat, dan kapitalisasi banyak berbasis asumsi skenario; konfirmasi kontrak sebelum re-run. Triage: results/_triage__oms000-jenis-produk__20260907-115730.md. Tidak ada order atau data master dibuat.
